# P1-A05 正式负结果独立复核记录

- 任务：`P1-A05-NEGATIVE-REVIEW`
- 复核：成员 B
- 候选提交：`7d5cab63ec130a8cf18a010bf9ea8e08d35351c1`
- 日期：2026-07-18
- 结论：负结果证据与停止决定通过复核；P1-A05 关闭

## 1. 复核范围

Development 未通过时，冻结规程要求立即保留 P1-A04 并停止 P1-A05；B 的第二次调度 replay 只属于全门禁通过分支。因此本轮没有再次运行 scheduler，而是在同一不可变候选提交上独立复核：

- 50 个训练 artifact、五个主 checkpoint 和 training state；
- 2,040 条 Development 记录的完整性、hash、失败与数据边界；
- 四个标准报告 artifact；
- P1-A04/P1-A05 同 seed×scenario 配对与 10,000 次 bootstrap；
- comparison 的六项 gate 和停止决定。

没有访问、复制、解包或物化 public test，没有运行 `claim-test-gate`。

## 2. Artifact 独立重算

| 检查 | 结果 |
| --- | --- |
| 训练 manifest artifact | 50/50 字节数和 SHA-256 一致 |
| 主 checkpoint | 5/5 文件/参数 hash 与 comparison 绑定一致 |
| training state | 5/5 存在、hash 正确、`completed_epoch=2` |
| Development 笛卡尔积 | 2040/2040，无重复键 |
| status / illegal / penalty | success 2040；illegal 0；penalty 0 |
| report manifest artifact | 4/4 字节数和 SHA-256 一致 |
| test 边界 | `test_accessed=false`、`public_test_loaded=false` |

正式 comparison 文件 SHA-256 为 `2388bee7a4c738d2557a05375a5ad26980c098ab735b6d6212de5ac86f25de9a`。

## 3. 独立 bootstrap

B 未调用 comparison helper，独立建立 `(slice, policy, seed, scenario)` 索引，按 scenario ID 排序，对五个 training seed 和每切片 30 个场景使用 `numpy.random.default_rng(20260717)`；每次同时有放回抽取 5 个 seed 索引和 30 个 scenario 索引，共 10,000 次。delta 固定为 P1-A05−P1-A04。

重算与正式 comparison 的均值/delta 完全一致；percentile 仅有 `1.7e-18–6.9e-18` 的浮点末位差，使用预先显式记录的绝对容差 `1e-15` 通过：

| Slice | 独立 delta | 独立 95% CI |
| --- | ---: | --- |
| ID validation | -0.005471 | [-0.019719, 0.002402] |
| size-OOD | -0.014733 | [-0.045177, 0.000027] |
| CCR-OOD | 0.013471 | [-0.010703, 0.084744] |
| system-OOD | 0.034067 | [-0.008762, 0.115679] |

size-OOD 的 candidate mean=`1.553568>1`，且 new−P1-A04 CI upper=`0.0000265>0`。两项冻结门禁均明确失败，不存在把末位浮点差解释为通过的空间。

## 4. 复核决定

B 确认机器决定 `retain_p1_a04_publish_negative_result_and_stop_p1_a05` 与冻结规则一致。批准关闭 P1-A05 的正式运行和评测阶段，保留完整负结果，继续使用 P1-A04。

这不是 G3 批准，也不授权 P1-06 或 public test。禁止以 ID 小幅改善、system mean 略小于 1、或 size 点估计比 P1-A04 小为理由开启第二候选；size 的绝对门禁和显著改善门禁均未通过。
