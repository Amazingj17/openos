# TriSched 任务日志

建立日期：2026-07-16  
维护人：成员 B 负责日常归档，成员 A 复核算法和实验记录  
关联文档：[双人任务清单](./双人任务清单.md)｜[赛题十五获奖工作规划](./赛题十五获奖工作规划.md)

> 本日志记录可验证事实，不用“基本完成”“效果不错”等模糊表述。仓库于 2026-07-16 执行 `git init`，因此更早的既有代码无法从 commit 还原原始作者；下文的 A/B 表示从现在开始的责任接收人，不追认历史作者身份。

## 1. 当前状态快照

| 项目 | 当前结论 | 证据 |
| --- | --- | --- |
| 当前阶段 | P1-A03 微型 CLI/目录事务已由 B 独立复核通过，下一步运行冻结正式 3-seed | [P1-A03 独立复核](./P1-A03独立复核记录.md) |
| 自动测试 | 远端干净 clone `187 passed in 10.33s`；独立事务注入通过 | 32-artifact hash、15 对 NPZ、失败目录不变和 34-artifact MLP 旧入口通过 |
| 数据规模 | smoke 仍为 24/10/20；公开 STG projection 冻结为 120/30/30，每图 50 tasks | `data/benchmarks/stg-rnc50-hetero-v1.json` |
| 学习方法 | 公开 STG 14 维 BC warm start + clipped PPO/GAE；legacy smoke 仍含 REINFORCE | `outputs/p1-a02-stg-ppo/`（不提交 Git） |
| 当前主指标 | 3-seed validation ratio `0.807240 / 0.623254 / 0.739086`；mean `0.723193` | test 未访问；2 seed PPO 改善，1 seed 回退 warm start |
| 合法性 | 3 × 30 validation 全部合法 | best failure/illegal action count 总计均为 0 |
| 当前性能结论 | validation 开发门禁通过，但不能宣称 PPO 稳定收益或 test 领先 | seed std `0.075948`；各 seed P95 ratio 均大于 1 |
| 平台状态 | 新增 openEuler 24.03 LTS-SP4 x86_64 用户态 + Python 3.11.6 + glibc 2.38 证据 | 内核仍为 WSL2，原生 openEuler VM 尚未验证 |
| 项目治理 | P1-03 已关闭；task-GNN 单变量契约继续冻结 14 维输入、奖励、split、3 seeds 与选模 | P1-A03 第一轮不得同时引入 resource-GNN、课程或新奖励 |

## 2. 两个人现在做什么

成员 B 已从远端不可变提交独立复核 P1-A03 CLI artifact、目录事务、MLP 隔离、参数量和 frozen train 内存并通过；成员 A 下一步运行冻结正式 3-seed validation。14 维输入、奖励、split、3 seeds、选模和回退规则继续冻结。

| 成员 | 当前任务 | 状态 | 今天应产出 | 完成后下一项 |
| --- | --- | --- | --- | --- |
| A | P1-A03：task-GNN 单变量对照 | 进行中 | 使用正式配置运行冻结 3 seeds，不访问 test | 输出 32-artifact run 与 MLP 配对报告 |
| B | P1-A03-REVIEW1：CLI 事务与证据复核 | 已通过 | 已完成远端 clone、独立注入、参数/内存和 MLP 复核 | A 正式 run 后复核结果包 |
| A+B | 互跑公开数据与学习闭环 | 已完成第十九轮 | B 独立通过 task-GNN CLI 事务且未访问 test | 只推进 task-GNN 正式单变量对照 |

## 3. 下一步顺序

1. 用户推送 B 的 P1-A03 独立复核记录。
2. A 使用 `configs/stg_task_gnn.json` 运行且只运行冻结 seeds `20260717/20260718/20260719`；test 继续禁止访问。
3. A 保留 32 项 artifact，并输出与既有 MLP 的逐实例配对、参数量、峰值内存、训练时长、正式 checkpoint CPU P50/P95 与失败切片。
4. B 从正式 run 的新不可变提交复核结果包；task-GNN 无配对优势或明显增加失败/时延时，立即回退 MLP，不扩展 resource-GNN。

## 4. 日志记录

### 2026-07-17｜P1-A03-REVIEW1｜B 独立复核 CLI 与目录事务

- 主责：B；被复核：A；状态：通过；完整记录见 [P1-A03 独立复核](./P1-A03独立复核记录.md)。
- 不可变来源：本地与 GitHub `main` 均为 `f6301ae7d017a6f554ebceef02dd2d3d4e819a7d`；全新 clone 工作树干净，完整回归 `187 passed in 10.33s`。
- 独立数据：B 未调用 A 测试辅助，另建 seeds 71/72/73 的 4/2/2 数据，生成 manifest 后物理删除 2 个 test 文件；训练和选模只加载 train/validation。
- 配置/产物：resource-GNN、新 reward、curriculum 三类注入均被拒绝；32 个 artifact 逐项重算 bytes/SHA-256 一致，manifest 固定 `test_accessed=false`。
- 恢复：seed 71 第二次 update 前中断，state 保持 epoch 1；连续/恢复 summary、3 curves 和 15 对 NPZ 逐数组一致，`resumed_seeds=[71]`。
- 事务：在 staging 写 seed 72 curve 时注入失败，正式目录逐字节不变，staging/backup 残留为 0。
- MLP 隔离：相对 `c84f4c5` 的 32 个既有 PPO 顶层单元均未改；独立 MLP run 仍为 `stg_masked_ppo`，声明 34 个 artifact。
- 资源预检：正式参数量 GNN/MLP 为 `1008/512=1.96875×`；120 train 形成 6000 transitions/139,848 candidates，唯一张量约 19.74 MiB，采样 RSS 峰值增量约 53.42 MiB，graph 恰按 Scenario 共享。
- 时间：无追踪器时 teacher `17.395 s`、冻结 `59.393 s`；随机初始化架构 CPU 单轮 P50 为 MLP/GNN `73.041/81.276 ms`，方差很大，只作开销预检。
- 过程异常：第一次 pytest 误在多个历史 clone 的父目录执行而跨目录收集失败；切换到本次 clone 根后 187 项全通过，确认不是代码失败。
- 结论：解除正式 3-seed 工程阻塞；当前仍无 task-GNN 性能结论，也未访问公开 test。
- 下一步：用户推送后由 A 运行冻结正式 validation；B 随后复核正式结果包和配对统计。

### 2026-07-17｜P1-A03-CLI｜A 提交独立 CLI 与目录事务

- 主责：A；复核：B；状态：待独立复核；契约见 [P1-A03 task-GNN 设计与验收](./P1-A03TaskGNN设计与验收.md)。
- 入口：新增 `configs/stg_task_gnn.json` 与 `train-task-gnn`，冻结 `task_gnn_v1/message_dim=8` 和规范 14 维 teacher-free schema；原 `train-ppo` 继续默认 MLP。
- 产物：每个 seed 输出 BC warm start、actor/value best/last、PPO state、curve、validation diagnostics/failures；run 级输出 resolved config、teacher/reference、summary 与 manifest，共 32 个被声明 artifact。
- 可追溯：resume contract 绑定 normalized config、原配置/benchmark/代码 hash、architecture、warm-start 参数、train/validation Scenario 与 teacher/reference hash，且固定 `test_accessed=false`。
- 微型隔离：使用 4 train / 2 validation，并物理删除 test raw 文件；CLI 首次发布记录 `direct_new_directory`，全部 artifact 的 bytes/SHA-256 可重算。
- 中断恢复：2 epochs 在 seed 41 第二次 update 前中断，state 保持 epoch 1；恢复与连续 run 的 summary、3 条曲线、12 个 actor/value checkpoint 和 3 个最终 state 逐数组一致，manifest 记录 `resumed_seeds=[41]`。
- 目录事务：恢复先复制到同盘 staging，全部 seed/summary/manifest 成功后交换目录；在 seed 42 曲线写出时注入后置失败，正式目录逐字节不变，staging/backup 残留为 0。
- 测试：新增 `tests/test_task_gnn_pipeline.py` 的 `3 passed`；完整回归 `187 passed`。配置注入拒绝 resource-GNN、课程字段和 14 维 schema 漂移；当前环境无 Ruff，已执行语法检查、Black check（新增测试）和 `git diff --check`。
- 边界：这只是微型闭环与事务证据，未运行正式 3-seed、未访问公开 test、没有 task-GNN 性能结论。
- 下一步：用户推送后由 B 从远端不可变提交独立复核；通过后才允许 A 运行冻结正式 validation。

### 2026-07-17｜P1-A03-RESUME｜A 提交完整 epoch 状态

- 主责：A；复核：B；状态：进行中；契约见 [P1-A03 task-GNN 设计与验收](./P1-A03TaskGNN设计与验收.md)。
- 状态：metadata + 56 个数值数组；覆盖 task-GNN 当前参数/Adam、best 参数、critic 当前参数/Adam、best critic、双 PCG64、history、best key/epoch 和 completed epoch。
- 身份：直接绑定 architecture、14 维 schema、hidden/message/value 维度、seed、外部 contract SHA-256、BC warm-start 参数 SHA-256 和 `test_accessed=false`。
- 原子文件：同目录临时 NPZ 经 flush/fsync 后 `os.replace`；每个完整 epoch 后才发布，异常时保留上一个完成边界。
- 中断：2-epoch 微型训练在第二次 update 前注入异常，状态保持 epoch 1；恢复后 summary、best/last actor、best/last critic 和最终 57 个 NPZ 条目与连续运行逐数组一致。
- 诊断：独立触发已存在、缺失、payload hash、contract、warm-start、额外数组和损坏状态，得到稳定 `ppo_resume_state_*` 错误；Adam step 与 history/minibatch 独立重算一致，56 数值数组名必须精确匹配。
- 测试：新增恢复测试通过；task-GNN/训练/恢复与既有 PPO 聚焦 `20 passed`，本地完整回归 `184 passed in 7.54s`。
- 边界：当前只保证单个训练状态文件原子更新，尚未生成 CLI artifact/run manifest，也未证明整个正式输出目录在后置失败时不变；test 未访问。
- 下一步：接入 task-GNN 配置/CLI、全部证据 artifact 和 P1-03 staging 目录发布/回滚。

### 2026-07-17｜P1-A03-TRAIN1｜A 提交微型 1-seed BC/PPO

- 主责：A；复核：B；状态：进行中；契约见 [P1-A03 task-GNN 设计与验收](./P1-A03TaskGNN设计与验收.md)。
- 隔离边界：只使用合成 4 train / 2 validation / 0 test、每图 5–6 tasks、seed 61；未读取公开 STG raw root 或 test。
- teacher：新增独立 `FrozenTaskGNNTeacherStates`，每步保存同源只读 graph、合法 actions、14 维候选和 target，复放 HEFT 后继续通过生产/独立 validator。
- BC：两 epoch validation ratio `2.656002480246 / 2.092007786142`，零失败；同 seed 两次 summary 和全部 best/last 参数一致。
- PPO：21 transitions，epoch 1 validation ratio 仍为 `2.092007786142`，零失败/非法动作，奖励恒等误差 `8.881784197001252e-16`；best 正确保持 epoch 0 BC warm start。
- transition：只含 frozen state、selected index、old log-probability、只读 critic state、advantage 和 return，不保存 live env；同 episode 共享 graph，minibatch 在冻结状态上重算 probability/梯度。
- 旧路径：原 MLP BC/PPO 聚焦回归继续通过，未改变其 frozen state、transition 或默认入口。
- 测试：新训练闭环 `4 passed`，task-GNN/训练与既有 BC/PPO 聚焦 `19 passed`，本地完整回归 `183 passed in 7.65s`；直接调用时 `gamma=0.99` 也被 `ppo_reward_contract` 拒绝。
- 边界：微型 ratio 明显差于 1，只能证明训练闭环，不能证明 GNN 性能；尚无 CLI、epoch 状态、断点恢复或正式 3 seeds。
- 下一步：增加 task-GNN 配置/CLI、checkpoint hash、完整 epoch 状态与 staging 事务恢复，验证连续/恢复逐字段一致。

### 2026-07-17｜P1-A03-GRAD｜A 提交解析梯度与冻结图状态

- 主责：A；复核：B；状态：进行中；契约见 [P1-A03 task-GNN 设计与验收](./P1-A03TaskGNN设计与验收.md)。
- 反向传播：从 masked score 回传候选输出/融合层，经候选 task id scatter-add 到节点 context，再通过 self、前驱、后继邻接转置回到节点编码；覆盖全部 10 组参数。
- 数学门禁：对 `temperature=1.3` 的 log-probability 和 entropy 分别逐元素做中心有限差分，所有参数在 `rtol=2e-5, atol=2e-7` 下通过。
- 优化器：实现梯度字典/shape/有限性检查、全局 norm 裁剪和 Adam ascent；测试证明目标 log-probability 上升，clone 逐数组保留 Adam、step 和 RNG。
- 冻结状态：新增同源只读 ranks/归一化上下文、task node、前驱/后继邻接、合法候选和 14 维特征；与 Scenario 内容 hash 绑定，错误图拒绝，live env 继续 step 后仍可逐数组重放原 probability。
- 测试：task-GNN 聚焦 `7 passed`，core/PPO 聚焦 `14 passed`，本地完整回归 `179 passed in 7.22s`。
- 边界：尚未接入 BC/PPO 训练入口或 epoch 恢复，不得运行正式 3 seeds，不得声称 GNN 已训练或优于 MLP；test 未访问。
- 下一步：扩展 frozen teacher/PPO transition、task-GNN BC warm start、actor checkpoint/state 和微型 1-seed 连续/恢复。

### 2026-07-17｜P1-A03-FORWARD｜A 提交 task-GNN 最小表示

- 主责：A；复核：B；状态：进行中；契约见 [P1-A03 task-GNN 设计与验收](./P1-A03TaskGNN设计与验收.md)。
- 单变量：候选仍为原 14 维 teacher-free 输入；节点只复用其中 workload/upward-rank/indegree/outdegree，唯一新信息为既有 DAG 邻接。
- 前向：一次 self + predecessor mean + successor mean 消息传递，节点 context 与原 task-resource 候选融合后，仅在环境合法 candidates 上 softmax。
- 参数：`h=8,g=4` 的聚焦模型为 232 参数，同 `h=8` MLP 为 128；计划正式 `h=32,g=8` 为 1008 对 512，后续必须实测 CPU P50/P95。
- checkpoint：NPZ 内嵌 architecture、两套特征 schema、维度、seed 和 10 组参数；加载拒绝 schema、shape 和有限性漂移。
- 测试：task-GNN 聚焦 `4 passed`，core/PPO 聚焦 `14 passed`，本地完整回归 `176 passed in 7.59s`。已证明合法 mask、同 seed 确定性、DAG 敏感性和逐参数 checkpoint 往返。
- 边界：尚无梯度、BC/PPO、epoch 状态或 validation 结果，不得声称 GNN 已训练或优于 MLP；test 未访问。
- 下一步：实现解析梯度和 frozen graph state，中心有限差分通过后接入微型 1-seed BC/PPO 与事务恢复。

### 2026-07-17｜P1-03-REVIEW2｜B 通过事务性恢复重验

- 主责：A；复核：B；状态：已通过；完整证据见 [P1-03 第二轮独立复核记录](./P1-03第二轮独立复核记录.md)。
- 远端：修复提交 `d27479fa0df6c6f5efef3f3463a9d8c79559c0d1` 的 [Actions run 29573624741](https://github.com/Amazingj17/openos/actions/runs/29573624741) 共 7 个 job、7 个 evidence artifact 全部成功。
- 无 test：重新下载 125,500-byte 固定 archive 并核对 SHA-256，随后删除 30 个 test JSON 和 archive；官方独立根为 120/30/0、150 JSON，微型训练根为 4/2/0、6 JSON。
- 正常恢复：连续/恢复 summary 逐字段一致，非 JSON artifact 与三份最终状态逐字节一致；34 个 artifact 全部通过 manifest；状态均为 28 arrays、两套 PCG64、epoch 2、Adam step 16。
- 发布证据：连续运行记录 `direct_new_directory`，恢复运行记录 `staging_directory_swap` 和 `resumed_seeds=[31]`。
- 故障事务：teacher、缺状态、配置、payload hash、损坏 NPZ、错误 seed、后置运行时、staging 复制、发布第二次重命名和缺输出共 10 类注入均给出预期结果；每次调用前后正式目录快照一致，发布失败回滚旧目录，残留为 0。
- 回归：隔离完整测试 `172 passed in 7.77s`，worktree 状态为空；独立脚本/报告 SHA-256 为 `4a6c4eeb... / 079868e3...`。
- 结论：P1-03 / P1-A02R 改为已完成；P1-A03 解除阻塞。下一唯一主任务为 A 的 task-GNN 单变量对照，test 继续禁止访问。

### 2026-07-17｜P1-03-FIX｜A 提交事务性 staging 恢复

- 主责：A；复核：B；状态：提交复核；前次失败证据见 [P1-03 独立复核记录](./P1-03独立复核记录.md)。
- 修复：`--resume` 不再直接写正式输出。入口先把现有证据目录复制到同盘唯一 staging，全部 seed、summary 和新 manifest 都只在 staging 内生成；成功后才将旧目录移为 backup、将 staging 发布为正式目录并清理 backup。
- 失败语义：配置、artifact、任一 seed 状态或训练/写出异常都会删除 staging 并重新抛出原诊断，正式目录不发生写入；创建 staging 和发布失败分别返回 `ppo_resume_stage_write`、`ppo_resume_publish`。
- 清单：resumed run manifest 新增 `publication_mode=staging_directory_swap`；非恢复新运行记录 `direct_new_directory`，状态格式、34-artifact 和既有恢复契约不变。
- 回归一：将 seed 32 状态替换为 seed 31 状态，实际返回 `ppo_resume_state_mismatch`；失败前后正式目录全部文件的相对路径和字节完全一致，无事务目录残留。
- 回归二：在 staging 已写完 seed 31、准备写 seed 32 曲线时注入运行时异常；失败前后正式目录全部文件逐字节一致，无 staging/backup 残留。
- 正常路径：既有连续/中断/恢复测试继续核对 summary 逐字段一致、三个最终状态逐字节一致、`resumed_seeds=[31]` 和 34 个 artifact。
- 测试：聚焦回归 `2 passed in 2.03s`；Windows 完整回归 `172 passed in 7.38s`；固定 openEuler 24.03 LTS-SP4 镜像中非 editable 安装后 `172 passed in 5.40s`；`git diff --check` 通过。
- openEuler 过程：首次临时容器遗漏锁定 `wheel`，在打包阶段以 `invalid command 'bdist_wheel'` 退出，测试未启动；补齐 `pip/setuptools/wheel` 并按正式脚本注入 Git provenance 后完整通过。该次是 dirty worktree 开发验证，不替代推送后的 clean-commit CI artifact。
- 边界：跨平台目录发布由同文件系统两次重命名和 rollback 组成，不宣称主机断电或进程恰在两次重命名之间被强杀时仍是单指令原子交换；若发生该极端情况，须检查正式目录同级的 backup/staging 后再恢复。
- 下一步：用户推送后确认远端 7 环境；B 不复用 A 的测试辅助，从新不可变提交重复正常恢复、两类后置失败和目录快照。通过前 P1-03 保持部分完成，P1-A03 保持阻塞。

### 2026-07-17｜P1-03-REVIEW｜B 退回 PPO 断点续训

- 主责：A；复核：B；状态：已退回；完整证据见 [P1-03 独立复核记录](./P1-03独立复核记录.md)。
- 远端：被复核提交 `cddca3b08bab037cc79b4d60cc91c98813c8d979` 的 [Actions run 29571557663](https://github.com/Amazingj17/openos/actions/runs/29571557663) 已完成；Windows/Ubuntu × Python 3.10/3.11/3.12 与 openEuler 24.03 LTS SP4 共 7 个 job、7 个 evidence artifact 全部成功。
- 无 test 边界：官方 archive 为 125,500 bytes、SHA-256 `03bc163c13ae8601f8cb20ac1573746a5262c50edbf0c4e9e748968675ea5f7d`；删除 test 和 archive 后为 120 train、30 validation、0 test。独立微型训练根为 4/2/0，只含 6 个 JSON。
- 正常恢复：3 seeds × 2 epochs 的连续/恢复 summary 与 JSON 逐字段一致，非 JSON 和三份最终训练状态逐字节一致；34 个 artifact 初始均通过 manifest；状态均为 28 arrays、两套 PCG64 RNG、`completed_epoch=2`、Adam step 16。
- 错误码：teacher、缺状态、配置、payload hash、损坏 NPZ、错误 seed 和缺输出目录分别返回预期稳定诊断；缺输出目录 CLI exit 2；test 始终未访问。
- 阻塞缺陷：在完成 run 上令 seed 32 状态不匹配后恢复，seed 31 曲线先被重写，随后 seed 32 才报错。`seed_31_training_curve.json` 从 manifest hash `da9a43f...` 变为 `8a791f3f...`，旧 manifest 立即失效。
- 根因：pipeline 按 seed 验证并立即写出，没有在任何写入前完成全部 seed 的只读 preflight，也没有 staging 后整体发布。
- 回归：隔离 worktree 完整测试 `171 passed in 7.57s`，受跟踪状态为空。测试全绿不覆盖上述独立后置失败序列，因此不改变退回结论。
- 下一步：A 实现全 seed preflight 或 staging 发布，并增加“任一后置 seed 失败时正式目录逐字节不变”的自动测试；修复推送后仍由 B 从新不可变提交复核。P1-03 与 P1-A03 当前均阻塞。

### 2026-07-17｜P1-03｜A 提交 PPO epoch 边界断点续训

- 主责：A；复核：B；状态：提交复核；产物见 [P1-03 断点续训设计与验收](./P1-03PPO断点续训设计与验收.md)。
- 接口：`train-ppo` 新增 `--resume`；首次运行和恢复必须使用同一配置、代码、数据和输出目录，缺失恢复目录返回 `ppo_resume_output_missing` 且不创建目录。
- 状态：每个 seed 在 epoch 0 和每个完整 PPO epoch 后保存 actor/value 参数、两个 Adam、actor/training RNG、history、best key/epoch 和 best 参数。
- 原子性：状态经同目录临时文件、flush、`fsync` 和原子替换发布；epoch 中途退出只会回退到上一完整 epoch。
- 绑定：状态契约覆盖配置/benchmark/code source、14 维特征、PPO 参数、warm-start 参数 hash、train/validation Scenario 与 teacher/reference hash，继续固定 `test_accessed=false`。
- 自校验：NPZ 内嵌 metadata+数组 payload SHA-256；加载时检查数组 shape/有限值、epoch/history、best 状态和 Adam step 与 update/minibatch 历史一致。
- 对照：微型 3-seed × 2-epoch 在 seed `31` epoch 2 前注入中断；恢复后的 summary 与曲线逐字段一致，三个最终训练状态与连续运行逐字节一致。
- 清单：新运行每 seed 增加一个训练状态，3-seed manifest 从历史 31 个 artifact 增为 34 个；run manifest 记录 `resume_requested`、`resumed_seeds` 和恢复边界。
- 注入：修改学习率、篡改 teacher、删除 seed 状态、篡改 metadata 保留旧 hash、损坏 NPZ、缺失恢复目录均被对应稳定错误码拒绝。
- 测试：完整回归 `171 passed in 6.88s`；test 原始字节继续缺失，正常和错误路径均不加载 test。
- 下一步：用户推送后由 B 从不可变提交独立中断/恢复与注入；通过前 P1-03 保持部分完成，不开始 task-GNN。

### 2026-07-17｜P1-A02-REVIEW｜B 独立复核 masked PPO/GAE

- 复核人：B；原主责：A；被复核远端提交：`0794d1822b2060861b9fc3ed2ffc1f70df82cae0`；实现提交：`1bab5fa7fb536ef1f752c395bbb5c832b0643133`。
- 远端：Actions run <https://github.com/Amazingj17/openos/actions/runs/29568806751> 的 Windows/Ubuntu × Python 3.10/3.11/3.12 与 openEuler 共 7 个 job 全部成功。
- 无 test 数据：固定 archive 为 125,500 字节、SHA-256 `03bc163c...`；删除 test JSON 和相关 archive 后只剩 120 train + 30 validation，test 0/30、JSON 总数 150。
- 正式复跑：约 699.5 秒；best ratio 为 `0.807240 / 0.623254 / 0.739086`，mean/std 为 `0.723193 / 0.075948`；全部 failure/illegal 为 0，2 seed PPO 改善、1 seed 回退 warm start。
- 风险边界：每个 seed 仍有 9/3/5 个 validation 实例劣于 HEFT，P95 为 `1.332844 / 1.227243 / 1.237521`；只能宣称 validation 门禁通过，不能宣称稳定 PPO 收益或 test 领先。
- 构件：31 个 artifact 的 bytes/hash 全部重算通过；A/B 有 29/31 个声明构件逐字节一致，另 2 个 teacher/reference manifest 只差代码提交元数据；B summary/run-manifest hash 为 `059a8576... / 6701b3a1...`。
- checkpoint：复载 3 seed 的 best/last actor/value 并重评 180 份 validation 调度，参数 hash 与指标逐字段一致；seed `20260718` 的 best actor 与 BC warm start 参数 hash 相同，证明 epoch 0 回退生效。
- 数学与注入：奖励 telescope、非零 value GAE、clipping 六分支均通过；正式最大恒等式误差 `1.78e-15`，最大 KL `0.00808 < 0.03`；teacher 特征泄漏和 `gamma=0.99` 均在训练前 exit 2 且不创建输出目录。
- 测试：隔离完整回归 `169 passed in 5.60s`，worktree 状态为空；产物见 [P1-A02 独立复核记录](./P1-A02独立复核记录.md)。
- 结论：P1-A02 改为已完成（B 独立复核通过）；P1-03 因断点续训尚缺而保持部分完成。下一主任务为 A 的 P1-A03 task-GNN，第一提交先补中断恢复门禁。

### 2026-07-17｜P1-A02｜提交 14 维 BC warm start 与 masked PPO/GAE

- 主责：A；复核：B；状态：提交复核；不可变实现提交：`1bab5fa7fb536ef1f752c395bbb5c832b0643133`。
- 特征：PPO 主路径强制删除 `is_heft_task/is_heft_pair`；配置缺任一排除项即以 `ppo_teacher_feature_leakage` 失败。16 维模型只作为同 seed BC 受控参考，不进入 PPO 或主选模。
- 奖励：每步负增量 makespan/HEFT，`gamma=1.0`，逐 episode 保证奖励和严格等于 `-final_ratio`；正式最大误差 `1.78e-15`。
- 算法：独立 actor/value MLP、GAE、clipped surrogate、entropy、Adam、梯度裁剪与 target-KL 早停；数值梯度最大误差不超过 `3.3e-10`。
- 数据：只加载 120 train + 30 validation；teacher/BC/PPO 梯度只用 train，checkpoint 和止损只用 validation，run manifest 固定 `test_accessed=false`。
- 消融：seed `20260717` 的 16 维 BC ratio/teacher accuracy 为 `1.0/1.0`；14 维为 `0.852539/0.514`，说明 improvement 不依赖直接读取 HEFT 动作位。
- 3-seed：best validation ratio 为 `0.807240 / 0.623254 / 0.739086`，mean/std 为 `0.723193 / 0.075948`；全部 30/30 合法且 failure/illegal 总计为 0。
- 选模：seed `20260717/20260719` 的 best 为 PPO epoch 2；seed `20260718` 的 PPO 退化，按冻结规则回退 BC warm start epoch 0。PPO 改善 2/3，不得写成稳定收益。
- 风险：三个 seed 分别仍有 9/3/5 个 validation 实例劣于 HEFT，按冻结线性插值口径计算的 P95 ratio 为 `1.332844 / 1.227243 / 1.237521`；test 未访问，没有最终领先结论。
- 清单：正式运行约 689.3 秒；31 个 artifact 大小/hash 全部重算通过；复载 6 组 best/last actor/value 并重新评测 180 份 validation 调度，指标逐字段一致。
- 关键 hash：summary `059a8576...`，run manifest `b4320d1b...`；正式输出位于 Git 忽略的 `outputs/p1-a02-stg-ppo/`。
- 测试：完整回归 `169 passed in 5.49s`；产物见 [P1-A02 设计与验收契约](./P1-A02MaskedPPO设计与验收.md)。
- 下一步：用户推送后由 B 在无 test 数据根上独立复跑与推导；通过前 P1-A02/P1-03 保持部分完成，不开始 task-GNN。

### 2026-07-17｜P1-A01-REVIEW｜B 独立复核 HEFT teacher 与公开 STG BC 基线

- 复核人：B；原主责：A；被复核远端提交：`180a7130517df425bf53dab945f3760e5bcdba72`；实现提交：`2eda5ae0eeca87affd54f4067ba17d4cdff1f766`。
- 远端：Actions run <https://github.com/Amazingj17/openos/actions/runs/29565244968> 的 Windows/Ubuntu × Python 3.10/3.11/3.12 与 openEuler 共 7 个 job 全部成功；7 个 artifact 复核时均未过期。
- 无 test 数据：重新下载官方 archive，SHA-256 为 `03bc163c...`；独立 raw root 只保留 120 train + 30 validation，确认 test 0/30、JSON 总数 150。
- 双次重跑：在完全缺少 test 字节的 raw root 连续运行约 185.6 秒和 184.6 秒；10 个核心构件逐字节相同，并与 A 的正式训练曲线、NPZ 和参数 hash 一致。两个 run manifest 仅创建时间不同，各自声明的 artifact 大小/hash 全部重算通过。
- teacher：train 120 场景/6,000 动作和 validation 30 场景/1,500 动作均零失败；聚合 trace/schedule hash 与正式值一致。
- 抽查：固定索引 `0,13,26,39,52,65,78,91,104,119` 的 10 个 train 场景在动作、生产/独立 HEFT、makespan、trace/schedule hash 和双 validator 上全部一致。
- 注入：test 用作 teacher/training/model selection 均返回 `split_usage`；trace 篡改返回 `teacher_trace_hash`；非法动作并同步重算 trace hash 后返回 `teacher_illegal_action`。
- checkpoint：best/last parameter hash 分别为 `fb3bba12...`、`343fde14...`；二者 validation 均为 30/30 成功、ratio 1.0、failure/illegal action 0、teacher accuracy 1.0。
- 测试：detached worktree 完整回归 `165 passed in 5.57s`，随后 `git status` 为空。
- 产物：[P1-A01 独立复核记录](./P1-A01独立复核记录.md)。临时 raw data、checkpoint 和注入证据仅位于 Git 忽略的 `outputs/`。
- 结论：P1-A01/P1-02 改为已完成（B 独立复核通过）；只证明复制 HEFT 的公开数据闭环成立，不证明性能领先。下一主任务为 A 的 P1-A02 masked PPO。

### 2026-07-17｜P1-A01｜提交 HEFT teacher 与公开 STG BC 基线

- 主责：A；复核：B；状态：提交复核。
- 实现基线：`2eda5ae0eeca87affd54f4067ba17d4cdff1f766`；正式运行的 run manifest 记录该 SHA、`working_tree_dirty=false`。
- 数据用途：新增 split/purpose 硬门禁；teacher/training 只允许 train，model selection 只允许 validation，test 用于前述用途均返回 `split_usage`。`train-bc` 实际只加载 train/validation，run manifest 固定 `test_accessed=false`。
- teacher：120 个 train 场景、6,000 个动作全部经过生产 HEFT、独立 HEFT 和双 validator；不一致/失败为 0。trace/schedule 聚合 hash 分别为 `51acb3c9...` 和 `96859e20...`。
- validation reference：30 个场景、1,500 个动作双签通过，失败为 0，只用于诊断与 checkpoint 选择。
- 训练：seed `20260717`、hidden 32、3 epochs、learning rate 0.004；teacher 状态特征一次物化后跨 epoch 只读复用，保持逐动作 Adam 目标不变。
- 选模：只按 validation 的零失败、mean ratio、teacher accuracy、earlier epoch 排序；epoch 1 即达到 ratio 1.0、teacher accuracy 1.0，因此选为 best，epoch 3 保留为 last。
- 合法性：best 在 validation 30/30 成功，failure/illegal action count 均为 0，valid rate 1.0；公开 test 完全未访问，没有 test 结果。
- checkpoint：best/last parameter SHA-256 分别为 `fb3bba12...`、`343fde14...`，两者不同；NPZ SHA-256 分别为 `fa5fa524...`、`1239ac1f...`。
- 清单：10 个声明构件逐一重算一致；summary/run manifest SHA-256 分别为 `3e9d9604...`、`98f5b1d8...`；teacher/validation failure JSONL 均为空。
- 测试：完整回归 `165 passed in 4.90s`；正式三轮运行约 194 秒。运行产物位于 Git 忽略的 `outputs/p1-a01-stg-bc/`。
- 限制：16 维特征直接含 `is_heft_task/is_heft_pair`，本结果只是 HEFT 复制 sanity baseline，不能宣称性能领先；仍缺 PPO、GNN、多 seed、OOD 和最终 test。
- 产物：`trisched/bc.py`、`configs/stg_bc.json`、`train-bc` CLI、用途门禁/缓存优化、两项新增测试文件及 [P1-A01 记录](./P1-A01HEFT教师与BC基线.md)。
- 复核要求：B 不复用 A 输出，只使用 150 个 train/validation 原始文件双次重跑、独立抽查 teacher、执行 test/trace 注入并重算 checkpoint/构件 hash；通过前 P1-A01/P1-02 保持部分完成。

### 2026-07-17｜P1-B01-REVIEW｜A 独立复核公开 benchmark 与冻结 split

- 复核人：A；原主责：B；不可变基线：`694078b04199724074ec85ef459a2a3ed5023cb1`。
- 远端：GitHub commits API 确认 `main` 为该 SHA；Actions run <https://github.com/Amazingj17/openos/actions/runs/29560592554> 的 Windows/Ubuntu × Python 3.10/3.11/3.12 与 openEuler 共 7 个 job 全部成功，7 个 artifact 均未过期。
- 来源与许可：重新查询 Zenodo `18927122`，独立下载得到 archive 125,500 bytes、MD5 `0d1a6b...`、SHA-256 `03bc163c...`；`LICENSE` 为 631 bytes、SHA-256 `bb96c8d...`，内容明确 CC BY 4.0 与原始 STG 署名。GitHub 固定提交根目录无许可证；results record `20419279` 的 rights 仍为空。
- 独立归档检查：181 个 member 中恰有 180 个互异 JSON 文件，无绝对/父目录/反斜杠或额外文件。
- 独立划分：对 180 个源文件重算 source hash 和带盐 rank，120/30/30 归属与 split index 全部一致；三组 Scenario hash 聚合值分别为 `145ea161...`、`64c5e67...`、`fd0f6bec...`，两两交集均为 0。
- 抽查：按每个 split 首/中/末加固定哈希选择 1 个的规则抽 10 项；不导入生产 loader 独立投影，10/10 source/Scenario hash、50 tasks、边数和 membership 一致。`rand0084` 的 `T4` 明确验证两条 edge data 分别来自前驱 `T1` 与 `T3`。
- 注入：源文件追加换行在加载前以 `source_hash` 失败；`../escaped.json` archive 以 `unsafe_archive_path` 失败，未产生逃逸文件。
- 标准链路：在线下载与离线复验通过；重建 manifest SHA-256 为 `69f156cc...` 且与提交文件逐字节相等；隔离完整回归 `163 passed in 5.23s`，worktree 保持干净。
- 产物：[P1-B01 独立复核记录](./P1-B01独立复核记录.md)。临时下载、抽样器和注入证据只在 Git 忽略的 detached worktree `outputs/` 中。
- 结论：P1-B01/P1-01 改为已完成（A 复核通过），P0-07 的去重、checksum 与防泄漏检查同时收口；下一主任务为 A 的 P1-A01 HEFT teacher 与 BC 稳定基线。

### 2026-07-17｜P1-B01｜提交公开 STG loader、许可证审计与冻结 split

- 主责：B；复核：A。
- 状态：提交复核，未标记完成。
- 论文与数据：核验 Grapheon RL 论文 DOI `10.1109/COMPSAC65507.2025.00341`；固定 Zenodo input record `18927122` 和 GitHub 提交 `22134e74028b0164032fbcc4c806bfa7c385e551`。
- 许可证：输入 JSON/包装/系统配置为 CC BY 4.0，license 文件 SHA-256 为 `bb96c8d...`，并声明原始 STG 获准再分发；固定 Sharma/Kunkel/Kasahara/Waseda 署名。GitHub 根无许可证、结果 record `20419279` 的 rights 未声明，二者代码/结果均不导入。
- 固定源：只使用 `rnc50_hetero_json.tar.xz`，125,500 bytes，MD5 `0d1a6b...`，SHA-256 `03bc163c...`；原始 archive 不提交 Git。
- loader：新增严格 UTF-8/有限值/task/dependency 校验；duration 映射 workload，edge data 取 predecessor data；显式忽略当前模型不能表达的 GPU/core/memory capability。
- projection：固定 device/edge/cloud 速度 1.0/2.4/4.8 和 MB/s 网络；命名为 STG topology projection v1，禁止宣称完整 GrapheonRL 系统复现。
- split：以带固定 salt 的 source SHA-256 排序，冻结 train/validation/test 为 120/30/30；180 个 source hash 和 Scenario hash 均唯一，三组交集为 0。
- split 聚合 hash：train `145ea161...`、validation `64c5e67f...`、test `fd0f6bec...`；manifest SHA-256 为 `69f156cc6a5a03124ed0e6c68dd6a84d502a4c470ad235a9bb2d7da9ddeae5cd`。
- 安全性：下载前后固定 byte/SHA；解包拒绝绝对/父目录路径、链接、特殊文件及成员增删；源字节和转换 hash 在使用前逐项校验。
- 实跑：在线下载/安全解包/180 项重算通过，随后 offline 复核可重复；全部 180 个 50-task 场景的 HEFT 结果同时通过生产与独立 validator。
- 测试：聚焦 `5 passed`；最终完整回归 `163 passed in 4.53s`；合成 smoke pipeline/checkpoint `mean_ratio=1.0`、总失败数 0，checkpoint hash 未变化。
- 产物：`trisched/benchmark.py`、两个 `scripts/*stg*` 脚本、`data/benchmarks/`、`tests/test_benchmark.py`、[设计与许可证记录](./P1-B01公开基准与许可证.md)。
- 限制：主 pipeline 尚未切换公开数据；STG 是公开合成 DAG；test 是冻结而非隐藏；能力约束未保留；上游无许可证代码/结果被排除。
- 复核要求：用户推送后，A 独立核验许可和 archive，并抽查 10 个实例、两类故障及三组 hash 交集；通过前 P1-B01/P1-01/P0-07 保持部分完成。

### 2026-07-17｜P0-08-REVIEW｜A 独立复核失败统计与 run manifest

- 复核人：A；原主责：B。
- 状态：已通过。
- 不可变基线：`git fetch origin main` 后，本地、`origin/main` 和 detached worktree 均为 `f7704dd9054595f8752d34b0e7e5a5cf041e3273`；隔离工作树复核前后均干净。
- 远端：Actions run <https://github.com/Amazingj17/openos/actions/runs/29553994781> 的 Windows/Ubuntu × Python 3.10/3.11/3.12 与 openEuler 共 7 个 job 全部 `success`；7 个 artifact 均存在、未过期，到期日为 2026-07-31。
- 独立方法：不读取或复用 B 的 `FlakyHeftRunner`；A 自行构造单任务 runner 和真实外部失败进程，覆盖全成功、混合、全失败与 HEFT 基线失败。
- 手算：混合 4 场景中 2 成功、2 失败，惩罚 5，`mean_ratio=(1+1+5+5)/4=3`；实际 failure/valid/tie/loss rate 均按预期为 0.5，CSV 4 行、JSONL 2 行、错误计数 2。
- 全失败：3 个 API 场景惩罚 7，`mean_ratio=7.0`、failure rate 1.0、成功均值 `null`；HEFT 失败返回 `scheduler_baseline_failed`。
- 真实 CLI：独立外部进程在 4 个 test 场景以 29 退出；结果仍完整写出 4 行 CSV、4 行 JSONL、摘要和 manifest，随后 CLI 以单行 `evaluation_contains_failures` 返回退出码 3。
- manifest：逐个重算 6 个 artifact 的 bytes/SHA-256，并核对配置、数据聚合 hash、checkpoint、依赖锁、评分口径、commit 和 clean 状态；失败 CLI manifest SHA-256 为 `7beb5e2077d9d8347f2c6f30647d1997007edd9e6480a09e3ddab04031638845`。
- 回归：detached worktree 完整测试 `158 passed in 4.80s`；标准 pipeline/checkpoint evaluate 的 masked MLP `mean_ratio` 均为 1.0、总失败数 0；checkpoint SHA-256 为 `7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`。
- 标准清单：pipeline/evaluate manifest SHA-256 分别为 `9810531e4cc28c99b7b3bab07af20d356d67be6bb4557a364ee7ea96ba1fcb3b` 和 `21c90e67581f622fd29b69f0fae6e118c665fd1d7c4ae99b9e20f301a8661215`，均记录 `f7704dd...`、dirty=false、source=git。
- 产物：[P0-08 独立复核记录](./P0-08独立复核记录.md)；临时证据位于 Git 忽略的 `outputs/p0-08-a-independent/`，不提交。
- 限制：默认惩罚 10.0 仍只是工程口径；Actions artifact 只有 14 天保留期；运行时间字段使本次产物 hash 不作为跨机器黄金值。
- 结论：P0-08 改为已完成（A 复核通过）；下一主任务为 P1-B01 公开 benchmark、许可证核验与冻结 split。

### 2026-07-17｜P0-08｜提交真实失败率与 run manifest

- 主责：B；复核：A。
- 状态：提交复核，未标记完成。
- 计分：新增 `evaluation.failure_penalty_ratio`，默认且 smoke 冻结为 10.0；失败的原始 makespan/ratio 留空，score ratio 使用惩罚值，失败仍保留在总样本分母。
- 统计：新增 success/failure count、真实 failure/valid rate、成功子集均值、惩罚后 win/tie/loss 和 error counts；全失败策略的成功均值为 `null`。
- 逐实例证据：CSV 增加 status/raw/score/penalty/error 字段；新增固定路径 `<split>_failures.jsonl`，无失败时为空文件。
- 基线：非 HEFT 失败逐实例记录并继续；HEFT 失败返回 `scheduler_baseline_failed`，不虚构 ratio 分母。
- CLI：带失败时先写完整结果和 manifest，再以单行 `evaluation_contains_failures` 返回退出码 3，阻止 CI/发布误判成功。
- manifest：pipeline/evaluate 均记录 commit、工作树 dirty 状态、运行时、原/解析配置 hash、数据 hash、checkpoint、依赖锁和每个声明 artifact 的 bytes/SHA-256；manifest 不自哈希；本地 openEuler 包装器拒绝 dirty 工作树后才注入 clean 来源。
- 测试：P0-08 聚焦 `26 passed in 2.49s`；完整回归 `158 passed in 4.51s`。
- 成功 smoke：pipeline/checkpoint 各 20 行，五个默认 scheduler failure rate 均为 0；masked MLP 惩罚后 mean ratio 为 1.0；11/6 个 manifest artifact 分别逐个重算通过；非 editable wheel 从系统临时目录复评通过。
- 全失败注入：不存在的 external scheduler 在 20/20 个场景失败；CSV 20 行、JSONL 20 行、failure rate 1.0、合法率 0、自定义惩罚及 mean ratio 均为 6.0；CLI 退出码 3、stderr 1 行。
- checkpoint SHA-256：`7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`。
- CI/openEuler：加入 commit 注入、零失败断言、manifest 逐 artifact hash 重算，并上传 manifest 与失败 JSONL。
- openEuler 实跑：在不可变实现提交 `121aa24e33743e70dfa34cabeaf8b8e36549bd11` 上，openEuler 24.03 LTS-SP4 用户态为 `158 passed in 4.23s`；pipeline/evaluate failure rate 均为 0，manifest 记录 commit 和 clean 状态正确，11/6 个 artifact 全部重算一致，容器残留为 0。
- 产物：`trisched/evaluation.py`、`trisched/cli.py`、`tests/test_evaluation_reporting.py`、[P0-08 契约文档](./P0-08失败统计与运行清单.md)；本地证据位于被 Git 忽略的 `outputs/p0-08-*`。
- 限制：默认惩罚 10.0 是工程口径；正式 benchmark 必须只在 validation 冻结。当前 timeout rate 可从 error counts 推导，尚未作为独立字段。
- 下一步：用户推送后确认七个远端 job；A 不复用 B 的 runner 独立构造混合和全失败场景，手算统计并重算 manifest，通过前 P0-08 保持部分完成。

### 2026-07-17｜P0-06-REVIEW｜A 独立复核外部 scheduler adapter

- 复核人：A；原主责：B。
- 状态：已通过。
- 远端基线：GitHub `main`、Actions run `head_sha`、本地 `main`、`origin/main` 和 detached worktree 均指向 `8b0d7d82d5aa5a2fdabfb3918b9ee30f82a36bcb`；两次 fetch 因连接重置/443 超时失败，已如实记录并用 GitHub API 交叉确认。
- 远端 CI：Actions run <https://github.com/Amazingj17/openos/actions/runs/29550101519> 的 Windows/Ubuntu × Python 3.10/3.11/3.12 与 openEuler 共 7 个 job 全部 `success`；7 个 artifact 均存在且未过期。
- 独立注入：不复用 B 的失败脚本，手写合法响应、0.35 秒超时、退出码 23、非法 JSON、合法 JSON/空调度和不存在命令；稳定错误码全部符合契约。
- 进程与 CLI：超时子进程 PID `16320` 在 adapter 返回后不存在；真实 CLI 启动失败的退出码为 3、stderr 恰为一行 JSON、stdout 为空。
- evaluator：单任务 HEFT 与手写 external makespan 均为 2.0，external ratio 为 1.0；CSV 三字段、示例 schedule 和双 validator 均通过。独立串行 external 在真实 CLI 的 20/20 个场景合法率为 1.0，mean ratio 为 `1.1763513213412533`。
- 回归：隔离 worktree `147 passed in 4.40s`；标准 pipeline 的 masked MLP test `mean_ratio=1.0`，五个默认 scheduler 合法率均为 1.0；checkpoint SHA-256 为 `7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`。
- 校验说明：Windows `core.autocrlf=true` 使 fresh checkout 为 CRLF；规范化为 Git canonical LF 后，四个关键源码 hash 与接口文档一致。
- 产物：[P0-06 独立复核记录](./P0-06独立复核记录.md)；本地忽略证据为 `outputs/p0-06-independent-review/`。
- 限制：当前 evaluator 仍 fail-fast；成功批次的固定合法率不是正式 failure rate；只验证直接超时子进程清理，不宣称任意后代进程树均被终止。
- 结论：P0-06 改为已完成（A 复核通过）；下一主任务为 B 的 P0-08 真实失败率、逐实例失败行和 run manifest。

### 2026-07-17｜P0-06｜提交外部 scheduler adapter 与统一 evaluator

- 主责：B；复核：A。
- 状态：提交复核，未标记完成。
- registry：新增 `trisched/schedulers.py`，把 HEFT、CPOP、Greedy-EFT、Random、masked MLP 适配为统一 whole-scenario runner；`evaluation.schedulers` 冻结有序且唯一的评测集合。
- 外部协议：每个 Scenario 启动一个 `shell=False` 子进程，stdin 输入版本化 UTF-8 JSON，stdout 返回完整 schedule；支持 `{python}`、配置相对工作目录、deadline 和 stdout 上限。
- 统一评测：内置、学习和外部 runner 共用 CSV、示例 schedule、ratio/runtime/CI 统计，并统一经过生产与独立 validator。
- 稳定诊断：覆盖配置、未知/重复名称、启动、超时、崩溃、过大输出、UTF-8、严格 JSON、响应 schema 和非法调度；CLI 输出单行 JSON，scheduler 错误退出码为 3。
- 参考程序：新增 `examples/external_heft_scheduler.py`；其调度轨迹与进程内 HEFT 一致，只验证协议边界，不作为独立算法基线。
- 测试：P0-06 聚焦 `8 passed in 1.48s`；Windows 完整回归 `147 passed in 3.97s`；openEuler 24.03 LTS-SP4 完整回归 `147 passed in 4.40s`，容器残留为 0。
- smoke：train/validation/test 为 24/10/20；五个默认 scheduler 的合法率均为 1.0；pipeline 与 checkpoint 复评的 masked MLP `mean_ratio` 均为 1.0。
- checkpoint SHA-256：Windows 为 `7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`；openEuler 为 `42f86f33b04329278f51a6b5b461a14c367bc3f6ac1f2b6308340bf204398926`；两者逐实例指标一致，压缩归档字节不作为跨平台相等判据。
- 产物：`trisched/schedulers.py`、`tests/test_external_scheduler.py`、`examples/external_heft_scheduler.py`、[外部调度器接口](./外部调度器接口.md)；运行证据位于被 Git 忽略的 `outputs/p0-06-*`。
- 已知限制：当前 fail-fast，尚未累计真实 failure rate；每场景进程启动开销计入 runtime；adapter 不沙箱化外部命令；完整失败统计属于 P0-08。
- 复核要求：A 不复用 B 的临时失败脚本，从远端不可变提交独立注入至少四类失败，核对 code、CLI 退出码 3、双 validator 和无残留子进程。
- 下一步：创建本地提交后由用户推送并确认 Actions；A 独立复核，通过前 P0-06 保持部分完成。

### 2026-07-17｜P0-10-REVIEW｜A 独立复核 openEuler CPU smoke

- 复核人：A；原主责：B。
- 状态：已通过。
- 远端基线：本地与 `origin/main` 均为不可变提交 `03058c582372f4ff80d329f7a259deda0d007b34`。
- 隔离方式：从 `origin/main` 创建 detached worktree；不读取 B 的输出，不复用 B 的容器、虚拟环境或 wheelhouse；依赖从网络重新下载到全新容器。
- 独立结果：`139 passed in 3.57s`；pipeline/checkpoint `mean_ratio` 均为 1.0，合法率 1.0；train/validation/test 为 24/10/20，hash 交集 0/0/0；checkpoint SHA-256 为 `42f86f33b04329278f51a6b5b461a14c367bc3f6ac1f2b6308340bf204398926`。
- 交叉检查：宿主机独立解析 JUnit 得到 139 项，pipeline/evaluate CSV 各 20 行，逐策略 count 与合法率正确，重算数据交集和 checkpoint hash 一致；容器残留为 0。
- 远端：Actions run <https://github.com/Amazingj17/openos/actions/runs/29547875139> 的 openEuler job 与原有六环境矩阵全部 `success`；artifact `evidence-openeuler-24.03-lts-sp4` 存在且未过期。
- 失败尝试：A 额外施加源码只读挂载时，setuptools 因不能创建 `build/` 失败；日志已保留。标准读写挂载的新容器随后通过；这说明只读源介质应改为预构建 wheel 安装。
- 边界：仍是 WSL2 内核上的 openEuler 用户态证据，不是原生 openEuler 内核；不支持性能领先声明。
- 产物：[P0-10 独立复核记录](./P0-10独立复核记录.md)。
- 结论：P0-10 通过并关闭；下一主任务为 B 的 P0-06 外部 scheduler adapter，A 复核失败路径。

### 2026-07-17｜P0-10｜提交 openEuler CPU smoke 与 CI

- 主责：B；复核：A。
- 状态：提交复核，未标记完成。
- 固定环境：`openeuler/openeuler:24.03-lts-sp4@sha256:17c15554be2a5bc46023acb6e04d609d77642b8c20e236e88deb18e41ae4558e`，openEuler 24.03 LTS-SP4 x86_64 用户态，Python 3.11.6，glibc 2.38。
- 从零复现：用户删除旧容器后，B 通过 `scripts/run_openeuler_smoke.ps1` 新建 `--rm` 临时容器并完整重跑；结束后无同名容器残留。
- 安装：容器内新建 `/opt/trisched-venv`，锁定依赖和非 editable 项目包安装成功，`pip check` 无错误；包从 `site-packages` 导入。
- 测试与指标：`139 passed in 3.10s`；train/validation/test 为 24/10/20；pipeline 与 checkpoint 复评的 masked MLP `mean_ratio` 均为 1.0、合法率 1.0；三组数据 hash 交集为 0/0/0。
- checkpoint SHA-256：`42f86f33b04329278f51a6b5b461a14c367bc3f6ac1f2b6308340bf204398926`。
- 实际变更：新增 Bash 容器执行脚本和 PowerShell 包装脚本；CI 增加固定 digest 的 openEuler job；新增 [P0-10 验证记录](./P0-10openEuler验证记录.md) 与 README 复现入口。
- 失败证据：第一次因 openEuler `everything` 仓库 404/504/SSL/超时失败，决定使用镜像自带 Python；第二次因 JUnit 根节点结构假设触发 `KeyError: 'tests'`，修复解析器后通过。两份日志保留在被 Git 忽略的本地输出目录。
- 边界：Docker Desktop 使用 WSL2 内核，因此只证明 openEuler 用户态兼容；远端 openEuler job 尚待用户推送后确认；学习策略仍不能宣称优于 HEFT。
- 下一步：创建本地提交后由用户推送；A 从远端不可变提交独立复跑并检查 Actions artifact，通过或退回 P0-10。

### 2026-07-16｜G1-REVIEW｜A 独立复核正确性门禁

- 复核人：A；原主责：B。
- 状态：已通过。
- 隔离方式：从不可变提交 `08acdcb6d87493d89dd438ce228456ff879f18a0` 创建干净 detached worktree，不读取 B 的 `outputs/g1-b-*`。
- 测试：G1 聚焦套件 `132 passed in 2.80s`，完整套件 `139 passed in 3.00s`；另行抽查 11 个高风险 node，`11 passed in 0.42s`。
- 抽查内容：G05 插入空隙、G06/G07 非对称通信、依赖违背、资源重叠、G03/G05 最优值与 Scenario CLI 缺字段诊断。
- 闭环：独立 pipeline/checkpoint 复评成功；train/validation/test 为 24/10/20，内容哈希交集为 0/0/0；checkpoint SHA-256 为 `7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`。
- 源码审计：双 validator、独立 oracle 隔离和精确 solver 上限符合评审包；合法率硬编码与外部 adapter 缺失均被准确列为后续项。
- 远端：`main` 已指向纯文档提交 `08acdcb6d87493d89dd438ce228456ff879f18a0`；代码基线 `7aa81e7` 的 Actions run <https://github.com/Amazingj17/openos/actions/runs/29504591482> 六个 job 全部 `success`。
- 产物：[G1 独立复核记录](./G1独立复核记录.md)。
- 结论：G1 改为已完成；下一任务为 P0-10 openEuler CPU smoke。该结论不关闭其余 P0，也不支持性能领先声明。

### 2026-07-16｜G1｜B 提交正确性门禁评审包

- 主责：B；复核：A。
- 状态：部分完成（提交 A 复核），尚未通过门禁。
- 证据范围：Scenario 输入契约、10 个 HEFT 黄金案例、独立 validator、160 份随机调度、10 类故障注入、CPOP、10 个小图精确参考、4 例独立资源顺序 oracle、pipeline/checkpoint 和跨平台 CI。
- 本地复现：G1 聚焦套件 `132 passed in 2.91s`；完整套件 `139 passed`；pipeline 和 checkpoint 复评的 masked MLP test `mean_ratio` 均为 `1.0000`。
- 数据检查：train/validation/test 为 24/10/20，三组内容哈希两两交集均为 0；checkpoint SHA-256 为 `7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`。
- 远端：基线 `7aa81e7ba7e7b5277c8e5f46527166b338956776` 的 Actions run <https://github.com/Amazingj17/openos/actions/runs/29504591482> 在 Windows/Ubuntu × Python 3.10/3.11/3.12 六个 job 全部 `success`。
- 风险分类：合法率固定为 1.0、静态模型边界、小图 solver 上限、CPOP 扩展规则、openEuler、公开 benchmark 和外部 adapter 均已记录；它们不推翻当前成功调度的正确性，但有明确后续完成点和禁止宣称范围。
- 产物：[G1 正确性门禁评审包](./G1正确性门禁评审.md)。
- B 结论：G1-C1 至 G1-C7 满足，建议 A 独立复核后通过；A 签字前 G1 仍为部分完成，禁止开始 PPO/GNN。

### 2026-07-16｜P0-03-REVIEW｜A 独立复核 Scenario 输入契约

- 复核人：A；原主责：B。
- 状态：已通过。
- 独立方法：手写三任务、双资源 payload，不读取 `tests/fixtures/invalid/scenario_cases.json`，不调用场景生成器；CLI 使用真实 Python 子进程。
- 异常覆盖：缺字段、嵌套未知字段、bool ID、原始 JSON 数字 `1e309`、矩阵行列错误、重复边、环图和 GBK 编码共 8 类；API 与 CLI 的 code/path 全部一致，非法退出码均为 2。
- 合法路径：Python API 构造成功；CLI 退出码为 0，`valid/scenario_id/task_count/resource_count/content_hash` 均符合预期。
- 测试：独立文件 `16 passed`，完整回归 `139 passed`。
- 闭环回归：pipeline 与 checkpoint 复评的 20 个 test 实例 `mean_ratio` 均为 `1.0000`、合法率均为 `1.0`；checkpoint SHA-256 为 `7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`。
- 远端：实现基线 `207b8eb08b5db64d954f77c215c04999776f825a` 的 Actions run <https://github.com/Amazingj17/openos/actions/runs/29503822505> 在 Windows/Ubuntu × Python 3.10/3.11/3.12 六个 job 全部 `success`。
- 产物：[P0-03 独立复核记录](./P0-03独立复核记录.md)、`tests/test_scenario_review.py`；测试文件 SHA-256 为 `01c650c4ab1b1dc0f643946e0fd20fe13d5f58da87e5c63bb25a18d111177c5c`。
- 结论：P0-03 改为已完成；下一项由 B 汇总 G1 门禁证据，A 独立签收，G1 前不开始 PPO/GNN。

### 2026-07-16｜P0-03｜提交 Scenario Schema 与结构化输入诊断

- 主责：B；复核：A。
- 状态：提交复核。
- Schema：新增 Draft 2020-12 `schemas/scenario.schema.json`，封闭顶层和嵌套对象，定义任务、资源、边和通信矩阵的必填字段、类型与基本范围。
- 运行时：新增 `ScenarioValidationError`，固定 `code/path/detail/to_dict()`；补齐 ID、矩阵、有限数、边端点/重复/自环、DAG 环检测，并严格拒绝未知字段。
- JSON：`Scenario.load` 拒绝 `NaN/Infinity` 和语法错误，报告行列；`save` 禁止生成非标准有限数。
- CLI：新增 `python -m trisched validate-scenario --input <path>`；合法返回 JSON 与退出码 0，非法返回结构化错误与退出码 2。
- fixture：固定 12 类非法 payload 及期望 code/path；结构层 6 类由 JSON Schema 拒绝，跨字段 6 类由运行时拒绝。
- 测试：新增 25 项，完整结果 `123 passed`；覆盖可选 UTF-8 BOM 与错误编码，保存/加载 hash 与既有 98 项均无回归。
- 产物：[Scenario 输入契约](./Scenario输入契约.md)、`schemas/scenario.schema.json`、`tests/fixtures/invalid/scenario_cases.json`、`tests/test_scenario_schema.py`。
- SHA-256：Schema 为 `cfa6a5250cc88154cf3e77b56bf2514fc1f1a5cac526524411419dbe454ce1cb`；其余见设计文档。
- 实现提交：`36edfc7`（`feat(scenario): [P0-03] add schema and structured diagnostics`）。
- 已知限制：JSON Schema 不能单独表达方阵边长、连续 ID、边范围和 DAG 无环，调用方仍必须使用 `Scenario.from_dict/load`；resource kind 现严格限定三类。
- 复核要求：A 独立构造至少 7 类未复用 fixture 的异常并核对 API/CLI；通过前 P0-03 不标为已完成。

### 2026-07-16｜P0-B03-REVIEW｜A 用资源顺序 Oracle 复核最优性

- 复核人：A；原主责：B。
- 状态：已通过。
- 独立方法：枚举全部任务资源分配和各资源任务全序，再用依赖/资源约束图最长路计算 makespan；不调用 B 的 ready-task 分支定界、生产环境或场景时间辅助方法。
- 最优值：G03/G05/G06/G07 的 A/B 结果分别一致为 `4.5/7.1/6.5/5.5`；最优资源分配也逐例一致。
- CPOP：四例关键路径、关键资源与 makespan 一致，分别为 `5.75/13.0/7.0/5.5`。
- 失败路径：9 任务上限和 `max_states=1` 均明确抛出 `ExactSolverLimitError`，未产生伪最优结果。
- evaluator：独立输出目录的 pipeline/checkpoint 复评成功；CPOP CSV 字段、20 个样本和 100% 合法率均存在。
- 测试：新增 4 个独立复核节点，完整结果 `98 passed`。
- 远端：Actions run <https://github.com/Amazingj17/openos/actions/runs/29501739630> 的 Windows/Ubuntu × Python 3.10/3.11/3.12 六个 job 全部 `success`。
- 产物：[P0-B03 独立复核记录](./P0-B03独立复核记录.md)、`tests/test_exact_review.py`。
- 结论：P0-B03 与 P0-05 改为已完成；G1 前先收口 P0-03 JSON Schema 和输入错误诊断。

### 2026-07-16｜P0-B03｜提交 CPOP 与小图精确 Oracle

- 主责：B；复核：A。
- 状态：提交复核。
- CPOP：新增 downward rank、`rank_u + rank_d` 优先级、确定性关键路径/关键资源和 `CpopPolicy`；已接入统一 evaluator、逐实例 CSV、示例调度与摘要统计。
- 精确 oracle：新增不导入生产 `env`/策略的 active-schedule 穷举分支定界；独立 HEFT 提供初始上界，当前 makespan 提供安全剪枝；超出 8 个任务或 2,000,000 状态时明确失败。
- 冻结结果：10 个黄金小图均记录解析 lower bound、已证明 optimal makespan、CPOP ranks/关键路径/资源及完整轨迹；G03 的 OPT/HEFT 为 `4.50/4.75`，G05 为 `7.10/13.00`。
- 随机属性：40 个固定 DAG 上的 HEFT、CPOP、Greedy-EFT、Random 共 160 份调度全部通过双 validator。
- 统一评测：CPOP validation/test mean ratio 分别为 `1.0170`/`0.9878`，合法率均为 100%；两者不能支持“稳定优于 HEFT”的结论。
- 测试：`python -m pytest -o addopts="" -q` 最终回归为 `94 passed`；完整 pipeline 与 checkpoint 复评成功。
- 产物：`trisched/exact.py`、`trisched/policies.py`、`tests/test_cpop_exact.py`、`tests/fixtures/golden/optimal_cases.json`、[CPOP 与小图精确 Oracle 报告](./CPOP与精确Oracle设计.md)。
- SHA-256：`optimal_cases.json` 为 `8767638dd491a051721b3c13f3ff8f7ce011a79f0a96fd561aa4530c1a40cfa1`；其余关键文件见设计报告。
- 实现提交：`424e53a`（`feat(scheduling): [P0-B03] add CPOP and exact small-DAG oracle`）。
- 已知限制：精确 solver 是指数复杂度，仅限小图；CPOP 多根/多出口采用本项目冻结的单关键链规则；未引入外部 CP-SAT/MILP 依赖。
- 复核要求：A 在查看冻结结果前独立手算 G03、G05 和一个非对称案例，并核对失败路径；通过前 P0-B03/P0-05 不标为已完成。

### 2026-07-16｜P0-B02-REVIEW｜A 独立复核调度 oracle

- 复核人：A；原主责：B。
- 状态：已通过。
- 远端基线：commit `154442c9f1da3852555ca0443177e0a8244699ab`，本地 `main` 与 `origin/main` 一致且工作树无任务外变更。
- 独立异常 1：父任务在 R0 于 `[0, 2]` 执行，边传输耗时为 `0.5 + 2/2 = 1.5`，子任务却在 R1 于 `[2, 3]` 执行；实际拒绝信息为 `dependency 0->1 is violated`。
- 独立异常 2：两个无依赖任务在单资源分别执行 `[0, 2]` 和 `[1, 3]`；实际拒绝信息为 `tasks overlap on resource 0: 0 and 1`。
- 独立性扫描：未发现 `trisched/oracle.py` 导入生产 `env`/`policies`，也未发现调用生产场景时间辅助方法。
- 本地验证：`python -m pytest -o addopts="" -q` 为 `80 passed in 0.51s`；完整 pipeline 的 test `mean_ratio=1.0000`。
- 远端验证：Actions run <https://github.com/Amazingj17/openos/actions/runs/29498001906> 对 Windows/Ubuntu × Python 3.10/3.11/3.12 的六个 job 全部 `success`。
- 结论：同意 P0-B02 和 P0-04 改为已完成；该结论只覆盖调度可行性及 HEFT 一致性，不覆盖全局最优性。
- 下一步：B 执行 P0-B03；A 先独立冻结小图 optimum/bound，再复核 CPOP 与参考 solver。

### 2026-07-16｜P0-B02｜提交独立 HEFT oracle 与调度验证器

- 主责：B；复核：A。
- 状态：提交复核。
- 独立实现：新增 `trisched/oracle.py`，不调用 `HeterogeneousDagEnv`、`Scenario.execution_time`、`Scenario.communication_time`、生产 upward rank 或 `HeftPolicy`；`run_policy` 现在依次执行生产和独立 validator。
- 三方差分：A 的 10 个手工 fixture、生产 HEFT 和 B 的独立 oracle 在 upward ranks、决策顺序、资源、开始/完成时间和 makespan 上逐例一致。
- 属性测试：固定 40 个随机 DAG（5–12 个任务、1–4 个资源、边概率 0.0–0.5），对 HEFT、Greedy-EFT、Random 共 120 份调度做独立检查，全部合法。
- 故障注入：缺任务、重复任务、未知任务/资源、非法/非有限时间、错误时长、依赖违背、资源重叠和 makespan 不一致共 10 类均被拒绝。
- 测试：执行 `python -m pytest -q`，实际结果 `80 passed`。
- 产物：`trisched/oracle.py`、`tests/test_oracle.py`、[独立验证器设计与测试报告](./独立验证器设计.md)。
- SHA-256：`trisched/oracle.py` 为 `e677cd6b6d77242faa6c593a310632285129c6273c2967e2ebbbf3748ebd24f2`；`tests/test_oracle.py` 为 `492ac11f26ba741312bbacd68bbeb21f91d7c165991c1533b1ca9915044949f1`。
- 实现提交：`8074e1e`（`feat(validation): [P0-B02] add independent scheduling oracle`）。
- 已知限制：只证明当前静态 DAG 语义下的调度可行性和 HEFT 一致性，不证明全局最优；P0-B03 仍需参考 solver。
- 复核要求：A 独立复跑测试，并另行构造依赖违背、资源重叠各一例；通过前 P0-B02 不标为已完成。

### 2026-07-16｜P0-A02-REVIEW｜B 用独立 oracle 复核黄金案例

- 复核人：B；原主责：A。
- 状态：已通过。
- 证据：10 个案例的手工期望、生产 HEFT 和不复用生产时间/排名/策略代码的独立实现逐字段一致，容差采用 fixture 的 `1e-9`。
- 高风险案例：G05 插入空隙，G06/G07 非对称方向，G09 零数据非零时延均一致。
- 结论：P0-A02 完成判据已满足，状态改为已完成；其 fixture 继续作为后续 CPOP/CP-SAT 和回归测试输入。

### 2026-07-16｜P0-A02｜提交十个 HEFT 黄金案例

- 主责：A；复核：B。
- 状态：提交复核。
- 产物：`tests/fixtures/golden/heft_cases.json`、`tests/test_golden_cases.py`、[黄金案例推导](./黄金案例推导.md)。
- 覆盖：链、fork、diamond、单资源、插入式空隙、双向非对称带宽、任务/资源 tie-break、零数据非零时延、断连子图。
- 期望值：每例固定 upward ranks、决策顺序、资源、开始/完成时刻和 makespan；容差 `1e-9`。
- fixture SHA-256：`e0fd853e7bfe97b465b591ec18247ecfff73a8c257a12cb0746aaeac294648a6`。
- 关键反例：G03 的合法替代调度 ratio 为 `0.9473684211`；G05 验证后决策任务插入早期空隙；G06/G07 只交换带宽方向即把 makespan 从 `6.5` 改为 `5.5`。
- 自动测试：新增 12 个测试节点，总计 `19 passed`。
- 提交：本地 commit `f0c6794`（`test(heft): [P0-A02] add ten hand-derived golden cases`）。推送时 GitHub 443 连接重置/超时，网络恢复后执行 `git push origin main`。
- 下一步：B 不复用当前 HEFT 逻辑，独立重算并逐例签字；通过前 P0-A02 不标记完成。
- 复核：待 B。

### 2026-07-16｜P0-B01-REVIEW｜A 独立复核工程基线

- 复核人：A；原主责：B。
- 状态：已通过。
- 远端证据：commit `05047776169df209ea2fd54be6ca7058d1f18def`；Actions run <https://github.com/Amazingj17/openos/actions/runs/29495805943>。
- CI：Ubuntu/Windows × Python 3.10/3.11/3.12 六个 job 全部 `success`。
- 独立复现：从 GitHub 全新 clone，创建新 `.venv`，锁定安装和非 editable wheel 构建成功；`pip check` 无错误，`7 passed`。
- pipeline：test `mean_ratio=1.0`，合法率 1.0。
- 结论：Apache-2.0、依赖锁、CI 和安装记录符合 P0-B01 验收；P0-B01 改为已完成。openEuler 继续作为 P0-10 独立验收。

### 2026-07-16｜P0-B01｜补齐许可证、CI、依赖锁和安装证据

- 主责：B；复核：A。
- 状态：提交复核。
- 许可证：选择 Apache-2.0，并写入 `LICENSE`、`pyproject.toml` 和 README。
- 依赖：新增 `requirements-lock.txt`，锁定 Python 3.10–3.12 的构建、运行、测试及条件依赖。
- CI：新增 Windows/Ubuntu × Python 3.10/3.11/3.12 六组合矩阵；运行测试、pipeline、checkpoint 复评并上传结构化证据。
- 隔离验证：在全新 `.venv` 中以非 editable wheel 安装，`pip check` 显示 `No broken requirements found`，pytest 为 `7 passed`。
- 运行结果：train/validation/test 为 24/10/20；pipeline 与加载 checkpoint 复评的 test `mean_ratio` 均为 `1.0`，合法率为 `1.0`。
- 产物：[安装验证记录](./安装验证记录.md)、`.github/workflows/ci.yml`、`requirements-lock.txt`、`LICENSE`。
- 提交：本地 commit `df075e3`（`chore(ci): [P0-B01] add license lockfile and CI`）。推送时 GitHub 443 端口连续连接超时/重置；网络恢复后执行 `git push origin main`，再检查 Actions 六环境矩阵。
- 已知边界：GitHub Actions 远端矩阵需在推送后确认；openEuler 不属于本任务，仍留在 P0-10。
- 下一步：推送后检查六个 Actions job；A 从远端全新 clone 并复核，通过后开始 P0-B02。
- 复核：待 A。

### 2026-07-16｜P0-A01-REVIEW｜B 独立复核算法与指标任务

- 复核人：B；原主责：A。
- 状态：已通过。
- 来源：从 GitHub 全新 clone `17db369ec756a004cbea12d646723d2fc1f2ef5a`，未使用原工作区输出。
- 测试：`7 passed`；重新运行 pipeline 后 test `mean_ratio = 1.0`。
- checkpoint 复评：20 个实例的 makespan 与 pipeline 结果逐项一致，合法率 1.0，SHA-256 为 `7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`。
- 手算复核：三个案例 HEFT makespan 分别为 `3.0`、`5.0`、`4.75`；案例 3 合法替代方案为 `4.50`，ratio 为 `0.9473684211`。
- 结论：同意 ADR 对当前模型“只证明闭环、不得宣称领先”的判断；P0-A01 改为已完成，A 可开始 P0-A02。

### 2026-07-16｜P0-A01｜审计算法基线与指标口径

- 主责：A；复核：B。
- 状态：提交复核。
- 实际变更：新增[算法与指标ADR](./算法与指标ADR.md)，冻结逐实例 `mean_ratio` 口径、数据/种子/CI/失败处理规则，记录当前实现偏差和 12 项模型局限。
- 手算证据：完成链式、fork 和带非对称通信的菱形 DAG 三个案例；案例 3 中 HEFT makespan 为 `4.75`，合法替代调度为 `4.50`，ratio 为 `0.94737`。
- 接口改进：新增 `python -m trisched evaluate`，可加载 checkpoint 复评且不重新训练；摘要记录 checkpoint SHA-256。
- 测试：执行 `python -m pytest -q`，实际结果 `7 passed`。
- 复评：加载本地 `outputs/smoke/masked_mlp.npz` 后，20 个 test makespan 与原结果逐实例一致，`mean_ratio = 1.0`；checkpoint SHA-256 为 `7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`。
- 提交：本地 commit `d84a781`（`feat(evaluation): [P0-A01] audit metrics and add checkpoint evaluation`）。连续三次推送因无法连接 GitHub 443 端口失败，网络恢复后执行 `git push origin main`。
- 结论：当前 MLP 只是 HEFT 复制基线，不能宣称领先；G1 前不开始 PPO/GNN。
- 下一步：B 按 ADR 第 8 节独立复评并签字；通过后 A 开始 P0-A02，将 3 个案例扩展为 10 个黄金案例。
- 复核：待 B。

### 2026-07-16｜P0-B01｜初始化并发布 Git 仓库

- 主责：B；复核：A。
- 状态：部分完成。
- 已完成：执行 `git init`，确认 Git 身份配置；创建首次提交 `fe99bd1c32196fb4be3fc234743ca4fc253561b1`，并推送 `main` 到 `https://github.com/Amazingj17/openos.git`。
- 本次发布范围：源码、测试、配置、README、规划/任务文档和官方赛题附件；`outputs/` 继续作为本地生成物忽略。
- 验证：首次提交前执行 `python -m pytest -q`，结果为 `6 passed`；本地 `main` 已跟踪 `origin/main`。
- 尚缺：LICENSE、CI workflow、依赖锁定和干净机安装记录，因此 P0-B01 不能标记为已完成。
- 下一步：选择许可证并建立 CI，然后由 A 独立 clone、安装和运行测试。
- 复核：待 A 独立 clone 并运行测试。

### 2026-07-16｜DOC-02｜建立双人任务管理文档

- 责任：B 维护任务清单和日志，A 复核算法/实验部分。
- 状态：已完成文档生成，待两名成员确认身份对应关系。
- 完成内容：建立双人固定分工、当前/下一步任务、P0/P1/P2 任务池、每日/每周检查及代码/实验/文档提交规范。
- 产物：[双人任务清单.md](./双人任务清单.md)、[任务日志.md](./任务日志.md)。
- 验证：Markdown 标题、表格、代码围栏和相对链接检查通过。
- 已知限制：成员真实姓名和正式提交日期尚未提供，因此暂用 A/B 和相对工期。
- 下一步：A 执行 P0-A01；B 执行 P0-B01；完成后分别追加结果记录。
- 复核：待 A、B 签收。

### 2026-07-16｜DOC-01｜将获奖规划转换为双人版 Markdown

- 责任：规划由 A、B 共同执行；B 维护工程进度，A 维护算法路线。
- 状态：已完成。
- 完成内容：Word 规划已转换为 Markdown；三人方案改为 A“算法与训练”、B“系统与评测”，并同步调整 12 周路线、前 14 天冲刺、P2 范围、交叉复核和单点风险。
- 产物：[赛题十五获奖工作规划.md](./赛题十五获奖工作规划.md)。
- 验证：15 个表格、8 个代码围栏、13 个链接和标题结构检查无错误；未发现三人分工残留。
- 下一步：执行 P0 基线接收，不直接跳到 GNN/PPO。
- 复核：待 A、B 签收。

### 2026-07-16｜MVP-02｜核验自动测试和 smoke 结果

- 责任接收：A 接收学习策略与训练结果；B 接收场景、环境、策略基线、评测和 CLI。
- 状态：MVP 验收通过；比赛性能未验收。
- 执行命令：

  ```powershell
  python -m pytest -q
  ```

- 实际结果：`6 passed`。
- 已核验 smoke 产物：本地 `outputs/smoke/summary.json`、`masked_mlp.npz`、逐实例 CSV、训练历史、数据 manifest 和完整配置；这些可重复生成的运行输出不提交 Git。
- 数据：train 24、validation 10、test 20；合成任务数 8–16；3 个异构资源。
- test 指标：masked MLP `mean_ratio = 1.0`、合法率 1.0、平均推理约 3.003 ms；HEFT 平均推理约 1.162 ms。
- 正确结论：学习策略复现了 HEFT 行为，证明训练—评测闭环可运行；当前没有优于 HEFT，也比 HEFT 推理更慢。
- 已知限制：单 seed、小规模合成数据、Windows 环境、无 PPO/GNN/OOD/公开 benchmark；不能作为最终主结果。
- 下一步：A 审计训练目标和行为克隆饱和问题；B 扩充独立正确性证据和平台验证。
- 复核：本次由工具复跑测试；仍需 A、B 各自独立复现并签字。

### 2026-07-16｜MVP-01｜最小可执行代码基线归档

- 责任接收：A 负责 `trisched/learning.py`；B 负责 `scenario.py`、`env.py`、`policies.py`、`evaluation.py` 和 `cli.py`；测试共同维护。
- 状态：已完成 MVP，P0 仅部分完成。
- 已有能力：

  - 参数化生成并校验异构 DAG 场景，支持 JSON 保存/加载和内容 hash；
  - ready task 与合法候选动作、跨资源通信、插入式时间线和调度校验；
  - HEFT、Greedy-EFT、Random 与统一 Scheduler Protocol；
  - masked MLP、HEFT 行为克隆、episodic REINFORCE、checkpoint 保存/加载；
  - 一键训练、验证、测试及结构化结果输出。

- 主要文件：[README.md](../README.md)、[smoke.json](../configs/smoke.json)、[test_core.py](../tests/test_core.py)。
- 未完成：LICENSE、Git/CI、JSON Schema、10 个黄金案例、属性测试、CPOP、CP-SAT、公开数据、PPO、GNN、openEuler。
- 下一步：先通过 G1 正确性门禁，再推进 P1。
- 复核：待 A、B 完成责任接收。

## 5. 后续日志模板

新记录放在“4. 日志记录”标题下方，按日期倒序追加；不得覆盖历史失败记录。

```markdown
### YYYY-MM-DD HH:mm｜TASK-ID｜任务标题

- 主责：A/B
- 复核：B/A
- 状态：进行中/提交复核/已通过/已退回/阻塞
- 目标：
- 实际变更：
- 复现命令：
- 测试实际输出：
- 指标前后对比：
- 产物路径与 SHA-256：
- 失败案例和限制：
- 决策与下一步：
- 复核结论、复核人、日期：
```

## 6. 状态变更规则

- `待开始 → 进行中`：主责人在本日志写明开始时间、输入和预计产物。
- `进行中 → 提交复核`：第 7.1 节要求的证据齐全，且复现命令由主责人实跑。
- `提交复核 → 已通过`：复核人独立执行关键命令并记录结果。
- `提交复核 → 已退回`：写明失败证据和修改要求，不删除原记录。
- `任意状态 → 阻塞`：写明阻塞条件、已尝试方案、需要谁在何时做何种决定。
- 任务状态变化时，必须同时更新[双人任务清单](./双人任务清单.md)中的对应状态。
