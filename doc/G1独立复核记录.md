# G1 正确性门禁独立复核记录

- 日期：2026-07-16
- 原主责：成员 B
- 复核人：成员 A
- 评审提交：`08acdcb6d87493d89dd438ce228456ff879f18a0`
- 代码基线：`7aa81e7ba7e7b5277c8e5f46527166b338956776`
- 结论：通过

## 1. 复核隔离与范围

成员 A 从不可变提交 `08acdcb` 创建干净的 detached worktree，工作树初始无变更，不读取成员 B 在主工作区生成的 `outputs/g1-b-*`。评审提交只修改 Markdown；被测代码与已通过远端矩阵的 `7aa81e7` 完全相同。

本次复核判断 [G1 评审包](./G1正确性门禁评审.md) 的 G1-C1 至 G1-C7 是否有充分证据，并核对 F1–F7 是否被如实归类。结论只覆盖当前静态、非抢占、无链路竞争 DAG 语义，不判断性能领先、公开数据泛化或 openEuler 交付。

## 2. 独立复现结果

### 2.1 聚焦与完整测试

```powershell
python -m pytest -o addopts="" -q tests/test_golden_cases.py tests/test_oracle.py tests/test_cpop_exact.py tests/test_exact_review.py tests/test_scenario_schema.py tests/test_scenario_review.py
python -m pytest -o addopts="" -q
```

- G1 聚焦套件：`132 passed in 2.80s`；
- 完整套件：`139 passed in 3.00s`。

A 另外按完整 node ID 定点运行 11 项高风险用例，结果为 `11 passed in 0.42s`：

- G05 插入式空隙以及 G06/G07 非对称通信的生产 HEFT；
- 同三例的生产/独立 HEFT 三方一致性；
- 跨资源依赖违背和同资源执行重叠拒绝路径；
- G03/G05 的资源顺序 oracle、精确 solver 与 CPOP 复核；
- Scenario 缺字段的真实 CLI 退出码和结构化诊断。

### 2.2 独立 pipeline 与 checkpoint

```powershell
python -m trisched pipeline --config configs/smoke.json --output outputs/g1-a-review
python -m trisched evaluate --config configs/smoke.json --checkpoint outputs/g1-a-review/masked_mlp.npz --split test --output outputs/g1-a-eval
```

| 策略 | test 数 | mean ratio vs HEFT | 成功批次合法率字段 |
| --- | ---: | ---: | ---: |
| HEFT | 20 | 1.0000 | 1.0 |
| CPOP | 20 | 0.9878 | 1.0 |
| Greedy-EFT | 20 | 1.0592 | 1.0 |
| Random | 20 | 2.9009 | 1.0 |
| masked MLP | 20 | 1.0000 | 1.0 |

checkpoint 加载复评未重新训练，20 个 test 实例的 masked MLP `mean_ratio = 1.0000`。新生成 checkpoint 的 SHA-256 为 `7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`，与 B 的结果一致。

train/validation/test 数量为 24/10/20；三组内容哈希的 train–validation、train–test、validation–test 交集分别为 0/0/0。

## 3. 源码边界审计

| 审计项 | A 的检查结果 | 判定 |
| --- | --- | --- |
| 双 validator | `run_policy` 在返回前依次调用生产 `validate_schedule` 和独立 `validate_schedule_independent` | 与 G1-C2 一致 |
| oracle 独立性 | `trisched/oracle.py` 只导入 `Scenario`，未导入生产 `env`/`policies`，且自行计算执行/通信时间 | 与 G1-C2/C3 一致 |
| 合法率字段 | `evaluation.py` 第 56 行仍固定写 `valid_schedule_rate: 1.0`，非法调度会在写摘要前使批次失败 | F1 准确；不阻塞 G1，但必须由 P0-08/P1-B02 修复 |
| 精确范围 | `solve_exact_schedule` 默认 `max_tasks=8`、`max_states=2_000_000`，越界抛出 `ExactSolverLimitError` | F3 准确 |
| 外部策略接口 | 未发现 scheduler registry/external adapter 实现，任务清单仍将 P0-06 标为部分完成 | F7 准确 |
| CI 触发规则 | `.github/workflows/ci.yml` 忽略纯 `doc/**` 和 `*.md` 提交 | `08acdcb` 无新 run 符合预期 |

静态模型、CPOP 多根/多出口规则、openEuler 缺口和公开 benchmark/多 seed/OOD 缺口均与源码、任务清单和设计文档一致，没有发现把这些限制伪装成已完成的情况。

## 4. 远端证据

GitHub 远端 `main` 已指向 `08acdcb6d87493d89dd438ce228456ff879f18a0`。该提交仅修改 Markdown，按 workflow 规则不会触发新 run。

代码基线 `7aa81e7ba7e7b5277c8e5f46527166b338956776` 对应的 GitHub Actions run <https://github.com/Amazingj17/openos/actions/runs/29504591482> 已完成；Ubuntu/Windows × Python 3.10/3.11/3.12 六个 job 全部 `success`。每个 job 均执行完整测试、pipeline、checkpoint 复评和结构化证据检查。

## 5. 复核结论

成员 A 同意 G1-C1 至 G1-C7 的证据和 B 的风险分类，签字通过 G1 正确性门禁。通过含义是：当前静态模型的输入、时间语义、合法性、HEFT/CPOP 与小图最优参考已有交叉实现和跨平台证据，可以结束本轮正确性阻塞。

本结论不等于 P0 全部完成，也不支持“当前模型优于 HEFT”。以下约束继续生效：

- 在 P0-08/P1-B02 修复前，不得把固定 `valid_schedule_rate` 当作正式失败率；
- 在 P0-10 完成前，不得宣称 openEuler 可复现；
- 在公开 benchmark、多 seed、OOD 和正式统计完成前，只能宣称当前闭环正确且 masked MLP 与 HEFT 持平；
- 任何环境时间语义、动态系统或精确 solver 范围变更都必须重新过 G1 核心测试。

下一任务固定为 P0-10 openEuler CPU smoke、CI 与干净机安装演练。PPO/GNN 不在本次提交中启动。
