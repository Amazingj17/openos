# P1-03 PPO 断点续训第二轮独立复核记录

- 任务：P1-03 / P1-A02R
- 原主责：成员 A
- 独立复核：成员 B
- 日期：2026-07-17
- 被复核远端提交：`d27479fa0df6c6f5efef3f3463a9d8c79559c0d1`
- 远端运行：<https://github.com/Amazingj17/openos/actions/runs/29573624741>
- 前次结论：[首轮独立复核未通过](./P1-03独立复核记录.md)
- 本轮结论：通过；P1-03 改为已完成，允许 A 开始 P1-A03 task-GNN

## 1. 复核目标与隔离边界

首轮复核已经证明正常连续/恢复轨迹一致，但发现后置 seed 状态失败会在报错前改写前序 seed artifact，使原 run manifest 失效。A 随后把整次 `--resume` 改为同盘 staging 事务。本轮只判断该修复是否满足以下门禁：

1. 正常连续/中断/恢复确定性和完整状态不得回退；
2. 任一后置状态、训练或写出错误不得改变正式输出目录；
3. staging 创建失败和目录发布失败必须有稳定诊断，发布第二步失败必须恢复旧目录；
4. 成功 run 继续声明 34 个 artifact，且 manifest 如实记录发布模式；
5. train/validation 之外的 test 原始字节不得存在或被访问。

B 在全新 detached worktree 中从远端不可变提交复核，没有复用 A 的 pytest helper、临时训练目录或故障注入函数。B 另写 Git 忽略的独立脚本，重新构造数据、训练、目录快照、payload hash 和发布失败注入。

## 2. 远端提交、CI 与 artifact

GitHub `main`、本地主工作树、隔离 worktree 与 `origin/main` 均指向：

```text
d27479fa0df6c6f5efef3f3463a9d8c79559c0d1
```

对应 Actions run 的以下 7 个 job 全部 `success`：

- Windows × Python 3.10 / 3.11 / 3.12；
- Ubuntu × Python 3.10 / 3.11 / 3.12；
- openEuler 24.03 LTS SP4 × Python 3.11。

run 同时生成 7 个 `evidence-*` artifact，复核时均存在且未过期，到期时间为 2026-07-31。远端证据只证明标准矩阵通过；本轮事务性结论来自下述独立故障序列和目录级快照。

## 3. 无 test 数据根

B 重新在线获取官方 archive，核对后得到：

```text
bytes=125500
sha256=03bc163c13ae8601f8cb20ac1573746a5262c50edbf0c4e9e748968675ea5f7d
```

随后按冻结 manifest 物理删除 30 个 test JSON 和包含 test 的 archive。独立数据根最终为：

```text
train=120
validation=30
test=0
raw_json_count=150
archive_present=false
```

B 从真实 STG JSON 构造 `4 train / 2 validation / 2 test` 微型 split，再删除其中 2 个 test JSON；实际训练根只有 6 个 JSON，计数为 `4/2/0`。正常路径、全部失败路径和 CLI 路径均保持 `test_accessed=false`。

## 4. 正常中断与恢复

独立配置使用 seeds `31/32/33` 和 2 个 PPO epoch。连续训练约 `8.264 s`；B 在 seed 31 的 epoch 2 更新前注入中断，前缀约 `2.793 s`，状态保持 `completed_epoch=1`；事务恢复约 `7.407 s`。

正常路径全部通过：

- 连续/恢复 `ppo_summary.json` 逐字段一致；
- 非 JSON artifact 逐字节一致，JSON artifact 逐字段一致；
- resumed manifest 声明的 34 个 artifact 均通过大小和 SHA-256 重算；
- 三份最终训练状态与连续运行逐字节一致；
- run manifest 记录 `resume_requested=true`、`resumed_seeds=[31]`、`resume_boundary=completed_ppo_epoch`；
- resumed run 记录 `publication_mode=staging_directory_swap`，连续新运行记录 `direct_new_directory`；
- test 原始文件不存在，`test_accessed=false`。

## 5. 状态内容独立审计

B 未调用生产 payload hash 函数，按规范化 metadata、排序数组名、shape 和 little-endian float64 字节独立重算三个最终状态：

| seed | payload SHA-256 | completed epoch | Adam step | arrays | RNG |
| ---: | --- | ---: | ---: | ---: | --- |
| 31 | `43182c3ee42b516ee56a3ca1abb1645d96d673ef1365b596f5b0b747759c9f0b` | 2 | 16 | 28 | PCG64 × 2 |
| 32 | `87794a828b887fe84d56c4c6373c34f6324222c53b3cb5e27ff20a8dd3b42858` | 2 | 16 | 28 | PCG64 × 2 |
| 33 | `49222b2d226df6e4b3de714a5c95db8c27f0b2fba2d26726d3bea61673ad0566` | 2 | 16 | 28 | PCG64 × 2 |

每个状态的 actor/value Adam step 均由 history 中的 update/minibatch 数独立重算为 16；best actor/value 参数逐数组等于状态中的 best 参数，参数 SHA-256 与 summary 一致。

## 6. 失败事务与目录快照

B 对每个故障在调用前记录正式输出内全部文件的相对路径、字节数和 SHA-256；调用失败后重新快照，并检查正式目录同级不存在 `.resumed.ppo-resume-*` 残留。

| 独立注入 | 实际结果 | 正式目录 | 事务残留 |
| --- | --- | --- | --- |
| teacher artifact 变化 | `ppo_resume_artifact_mismatch` | 调用前后相同 | 0 |
| seed 31 状态缺失 | `ppo_resume_state_missing` | 调用前后相同 | 0 |
| PPO 配置变化 | `ppo_resume_config_mismatch` | 调用前后相同 | 0 |
| metadata 变化但保留旧 hash | `ppo_resume_state_hash` | 调用前后相同 | 0 |
| NPZ 损坏 | `ppo_resume_state_read` | 调用前后相同 | 0 |
| seed 32 使用 seed 31 状态 | `ppo_resume_state_mismatch` | 调用前后相同 | 0 |
| seed 31 已在 staging 写完后，seed 32 写曲线时报错 | 原运行时错误重新抛出 | 调用前后相同 | 0 |
| staging 复制失败 | `ppo_resume_stage_write` | 调用前后相同 | 0 |
| 旧目录已移至 backup，staging 发布重命名失败 | `ppo_resume_publish`，旧目录回滚 | 调用前后相同 | 0 |
| 输出目录缺失 | `ppo_resume_output_missing`，CLI exit 2 | 不创建目录 | 0 |

每项注入恢复原始故障字节后，B 再按成功 run manifest 重算全部 34 个 artifact，最终仍全部一致。首轮出现的 `seed_31_training_curve.json` hash 漂移没有复现；失败套件结束后整个正式目录与故障前原始快照完全相同。

## 7. 完整回归与本地证据

隔离 worktree 执行：

```powershell
python -m pytest -o addopts= -q
git status --porcelain --untracked-files=all
```

结果为 `172 passed in 7.77s`，受跟踪状态为空。复核脚本和报告位于 Git 忽略的 `outputs/`，不提交仓库；其 SHA-256 为：

```text
p1-03-independent-rereview.py:
4a6c4eeb6296d0f7ed6c0fc31b3460822d6055de51d22847a2cfec445d9cafc1

review_report.json:
079868e343958f8ffee84efe57d64bd18e01f4e877bcd73e93578ffc07569a49
```

## 8. 结论与保留边界

B 同意 P1-03 通过第二轮独立复核。首轮阻塞缺陷已经由 staging 事务修复，正常确定性、状态完整性、错误诊断、目录不变性、发布回滚、无 test 边界和跨平台 CI 同时满足。因此 P1-03 / P1-A02R 改为已完成，A 可以开始 P1-A03 task-GNN。

以下边界继续保留：

- 本轮验证的是同一进程可观测异常和发布第二步失败回滚，不宣称主机断电或进程恰在两次目录重命名之间被强杀时仍是单指令原子交换；
- staging 会复制完整证据目录，正式大模型训练前仍需报告额外磁盘空间和发布耗时；
- test 完全未访问，本结论不包含最终 test/OOD 或性能领先证据；
- P1-A03 只能替换 MLP 表示，必须继续冻结 14 维基础输入、奖励、split、3 seeds、选模与 warm-start 回退规则。
