# G1 正确性门禁评审包

- 日期：2026-07-16
- 主责：成员 B
- 复核：成员 A
- 评审基线：`7aa81e7ba7e7b5277c8e5f46527166b338956776`
- 当前状态：已通过（成员 A 独立复核）

## 1. 门禁范围与判定

G1 只回答一个问题：在当前冻结的静态、非抢占、无链路竞争 DAG 语义下，Scenario 输入、环境时间语义、调度合法性、HEFT/CPOP 基线和小图最优参考是否已有相互独立、可复现的正确性证据。

G1 不证明当前学习策略优于 HEFT，不关闭 openEuler、公开 benchmark、正式失败率统计、外部 scheduler adapter、OOD 或最终发布任务。成员 A 现已签字通过，但只解除当前静态正确性阻塞；PPO/GNN 仍须等待下一项 P0-10，并按 ADR 顺序推进。

| 门禁项 | 通过标准 | 当前证据 | B 判定 |
| --- | --- | --- | --- |
| G1-C1 输入契约 | 合法输入稳定加载；非法结构/语义有固定 code/path 和 CLI 退出码 | P0-03 的 12 类固定 fixture；A 独立构造 8 类异常；实现与复核共 41 项测试 | 满足 |
| G1-C2 时间与合法性语义 | 任务唯一、依赖/通信、资源互斥、时长和 makespan 均由独立路径重算 | 生产 validator + `trisched/oracle.py` 独立 validator；所有策略执行出口强制双检 | 满足 |
| G1-C3 HEFT 一致性 | 10 个手算小图的 rank、顺序、资源、时间和 makespan 与两套实现一致 | 人工 fixture、生产 HEFT、独立 HEFT 三方逐字段一致 | 满足 |
| G1-C4 属性与失败路径 | 随机 DAG 无非法调度；主要错误不能静默通过 | 40 个固定 DAG × 4 策略 = 160 份调度；10 类故障注入；A 另构造依赖违背和资源重叠 | 满足 |
| G1-C5 最优参考与 CPOP | 小图最优值可证明；搜索超限明确失败；CPOP 轨迹可复核 | 10 个小图冻结 optimum/bound；A 用资源分配 × 资源顺序 oracle 独立复核 4 例；两类超限失败 | 满足 |
| G1-C6 集成与跨平台 | 完整测试、pipeline、checkpoint 复评和六环境 CI 成功 | 本地 139 项完整测试；远端 Ubuntu/Windows × Python 3.10/3.11/3.12 全部成功 | 满足 |
| G1-C7 结论边界 | 不把合成 smoke、启发式或小图证明夸大为性能/泛化结论 | ADR 与各复核记录明确限制；当前 masked MLP 只与 HEFT 持平 | 满足 |

## 2. 证据链与交叉复核

| 任务 | 主产物与实现 | 独立复核 | 结论 |
| --- | --- | --- | --- |
| P0-A02 HEFT 黄金案例 | `f0c6794`；`heft_cases.json`；[黄金案例推导](./黄金案例推导.md) | B 的 `independent_upward_ranks`、`independent_heft_schedule` 与人工/生产结果逐字段比较 | 已通过 |
| P0-B02 独立 validator | `8074e1e`；`trisched/oracle.py`；[独立验证器设计](./独立验证器设计.md) | A 独立构造跨资源依赖违背和单资源重叠，检查代码依赖隔离 | 已通过 |
| P0-B03 CPOP/精确 oracle | `424e53a`；`trisched/exact.py`；[设计报告](./CPOP与精确Oracle设计.md) | A 用不同搜索空间的资源顺序 oracle 复核 G03/G05/G06/G07 | 已通过 |
| P0-03 Scenario 契约 | `36edfc7`；Schema、结构化诊断、非法 fixture；[输入契约](./Scenario输入契约.md) | A 不读取 B fixture，手写 payload 并通过真实 CLI 子进程复核；[记录](./P0-03独立复核记录.md) | 已通过 |

上述证据不是同一实现的重复运行：人工预期、生产调度器、独立 HEFT/validator、ready-task 精确 solver、资源顺序 oracle 和黑盒 CLI 复核使用了不同的数据来源或算法结构。

## 3. 本次 B 汇总复现

### 3.1 聚焦与完整测试

```powershell
python -m pytest -o addopts="" -q tests/test_golden_cases.py tests/test_oracle.py tests/test_cpop_exact.py tests/test_exact_review.py tests/test_scenario_schema.py tests/test_scenario_review.py
python -m pytest -o addopts="" -q
```

- G1 聚焦套件：`132 passed in 2.91s`；
- 完整套件：`139 passed`；
- 聚焦套件覆盖输入契约、黄金案例、独立 validator、属性/故障注入、CPOP、精确 oracle 和两轮独立复核。

### 3.2 pipeline 与 checkpoint 复评

```powershell
python -m trisched pipeline --config configs/smoke.json --output outputs/g1-b-review
python -m trisched evaluate --config configs/smoke.json --checkpoint outputs/g1-b-review/masked_mlp.npz --split test --output outputs/g1-b-eval
```

| 策略 | test 样本数 | mean ratio vs HEFT | 双 validator 后合法率字段 |
| --- | ---: | ---: | ---: |
| HEFT | 20 | 1.0000 | 1.0 |
| CPOP | 20 | 0.9878 | 1.0 |
| Greedy-EFT | 20 | 1.0592 | 1.0 |
| Random | 20 | 2.9009 | 1.0 |
| masked MLP | 20 | 1.0000 | 1.0 |

checkpoint 独立加载复评没有重新训练，20 个实例的 masked MLP `mean_ratio = 1.0000`、合法率字段为 `1.0`。checkpoint SHA-256 为 `7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`。

数据 manifest 记录 train/validation/test 为 24/10/20 个实例；三组内容哈希两两交集均为 0。本地运行环境为 Windows、Python 3.11.7、NumPy 1.26.4。

### 3.3 远端矩阵

评审基线对应 GitHub Actions run <https://github.com/Amazingj17/openos/actions/runs/29504591482>。以下六个 job 均为 `success`：

- Ubuntu：Python 3.10、3.11、3.12；
- Windows：Python 3.10、3.11、3.12。

每个 job 都执行锁定依赖安装、完整 pytest、训练—评测 smoke、checkpoint 加载复评和结构化证据检查。

## 4. 不阻塞 G1、但不得遗漏的后续项

| 编号 | 已知边界 | 为什么不阻塞本门禁 | 强制处理点 |
| --- | --- | --- | --- |
| F1 | `evaluation.py` 的 `valid_schedule_rate` 仍对成功批次写固定 1.0，非法调度会使整批异常退出 | 成功结果在写摘要前已由生产与独立 validator 双检，不会把非法调度写成成功 | P0-08/P1-B02 必须保留失败行并计算真实失败率；修复前不得用该字段宣称正式失败率 |
| F2 | 模型不含链路竞争、抢占、资源故障和动态带宽 | validator 与环境遵循同一已冻结静态模型，门禁判断没有越界 | 任何动态扩展必须重新过黄金、属性和独立 validator |
| F3 | 精确 solver 只支持不超过 8 个任务，且是自研穷举/分支定界而非外部 CP-SAT/MILP | 小图最优参考有穷尽性论证、独立搜索空间复核和明确超限失败 | 对外表述必须写“小图精确 oracle”，不得泛化到训练规模 |
| F4 | CPOP 多根/多出口采用项目冻结的单关键链规则 | 规则、tie-break 和轨迹已明确且可复现 | 对比外部实现时必须披露该扩展 |
| F5 | 尚无 openEuler 证据 | G1 是语义正确性门禁，平台交付由 P0-10 单独验收 | G1 通过后的下一任务必须是 P0-10 |
| F6 | 仍缺公开 benchmark、OOD、多 seed 和正式 bootstrap 统计 | G1 不判断性能领先或泛化 | 在这些证据完成前只能宣称“闭环正确、当前与 HEFT 持平” |
| F7 | 外部 scheduler registry/adapter 与正式失败报告尚未收口 | 当前内置策略已经走统一 evaluator 和双 validator | 在接入外部策略或正式性能实验前完成 P0-06/P0-08 |

## 5. B 的门禁建议与 A 的复核清单

成员 B 认为 G1-C1 至 G1-C7 已满足，建议成员 A 独立复核后通过 G1。成员 A 已按下列清单完成复核并签字通过，完整结果见 [G1 独立复核记录](./G1独立复核记录.md)。

A 的最低复核清单如下（现已全部完成）：

1. 从远端 `7aa81e7` 或其后仅含本评审文档的提交开始，不使用 B 的本地输出目录；
2. 运行 132 项 G1 聚焦套件和完整测试；
3. 抽查 G05 插入空隙、G06/G07 非对称方向、依赖违背、资源重叠、G03/G05 最优值和一类 Scenario 结构化错误；
4. 重新运行 pipeline 与 checkpoint 复评，核对样本数、内容哈希交集、checkpoint hash 和双 validator 执行路径；
5. 核对 F1–F7 是否被准确归类，尤其不得把固定合法率字段或合成 smoke 用作正式领先证据；
6. 明确记录“通过”或“退回”、复核人、日期和失败证据。

A 已签字通过，任务清单同步把 G1 改为“已完成”；下一任务为 P0-10 openEuler CPU smoke。若后续任一核心 invariant、独立复核结果或远端矩阵失败，立即重新打开 G1 并暂停模型扩展。
