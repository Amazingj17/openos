# P1-A04 五种子扩展独立复核记录

- 任务：P1-A04-5SEED-REVIEW
- 原主责：成员 A
- 独立复核：成员 B
- 日期：2026-07-18
- 复核提交：`8692b618a12ea42805df66c0f0d20b175c948aef`
- 结论：通过

## 1. 独立环境与范围

B 在用户推送后执行 `git fetch origin main`，确认
`HEAD=origin/main=8692b61`，再建立 detached worktree：

```powershell
git worktree add --detach outputs/p1-a04-b-review-worktree origin/main
```

复核工作树未导入 A 的测试 helper，Git 状态保持干净。复核范围包括：

- seed-extension 配置与目标目录创建前的拒绝路径；
- 来源 3-seed 包、目标 5-seed 包和 manifest 的独立 hash 重算；
- 旧三 seed 是否逐字节继承；
- 两个新 checkpoint、训练状态和逐实例 validation 的独立复评；
- 在物理不存在 test JSON 的数据根上按正式预算重跑；
- 普通 masked PPO、resume 和 task-GNN 兼容性。

本轮没有运行正式 development/OOD，也没有读取 public test。

## 2. 代码与合同审查

B 逐段检查 `trisched/ppo.py` 后确认：

1. `reuse_seeds` 必须是目标 seed 的非空严格前缀，且至少新增一个 seed；
2. 来源 manifest 固定 SHA-256 在目标目录创建前验证；
3. 来源 manifest 声明的全部 artifact 均重算字节数与 SHA-256；
4. 来源与目标训练合同仅允许 seeds、output_dir 和 seed_extension 不同；
5. 来源 config/manifest/summary 的 seed 顺序和
   `test_accessed=false` 均被验证；
6. 旧 seed 只复制标准 BC、best/last actor/value、curve、diagnostics、
   failures，以及来源存在时的 training state；
7. summary/run manifest 明确区分
   `inherited_verified` 与 `trained_current_run`；
8. 非扩展 PPO 不写空 extension 字段，task-GNN 仍拒绝未审变量；
9. extension resume 在 staging 中校验已继承文件，不改写来源包。

## 3. Detached 自动回归

实际命令与输出：

```text
tests/test_ppo_baseline.py:
8 passed in 3.68s

tests/test_task_gnn_training.py + tests/test_task_gnn_pipeline.py:
8 passed in 2.40s

full suite:
230 passed in 12.30s

compileall / git diff --check / detached git status:
passed / passed / clean
```

专项测试证明只训练两个新增 seed，前三个 seed 九类 artifact 逐字节不变，
来源 checkpoint 篡改会在目标目录创建前被拒绝。

## 4. A 正式包的独立静态与行为审计

审计对象：

- 来源：`outputs/p1-a02-stg-ppo/`
- 目标：`outputs/p1-a04-stg-ppo-5seed/`
- 来源 manifest SHA-256：
  `b4320d1bd880434cb16a9bd6cbf69ae441e8ca9fcbdfb8aaefd77de43513f594`
- 目标 manifest SHA-256：
  `bf36b872f8806dbbbe731fa24a748cdcb2e63efea0653b3106a22d546c732791`

独立脚本不调用 `_prepare_seed_extension()`，而是从 JSON 和文件字节直接
重算：

| 检查 | 结果 |
| --- | --- |
| 来源 artifact | 31/31 字节数与 SHA-256 正确 |
| 目标 artifact | 50/50 字节数与 SHA-256 正确 |
| 旧三 seed 必需文件 | 24/24 与来源逐字节一致 |
| 来源 manifest 快照 | 与来源原文件逐字节一致 |
| seed/provenance 顺序 | 3 inherited + 2 trained，顺序正确 |
| 数据访问声明 | 只含 train/validation，test=false |
| A 正式代码 provenance | `a38c530...`，working tree clean |

B 使用 detached 代码加载两个新 best actor/value，并在 30 个 validation
场景上分别复评：

| Seed | 独立 mean ratio | 逐实例行 | failure | illegal |
| --- | ---: | ---: | ---: | ---: |
| 20260720 | 0.6583984179686692 | 30 | 0 | 0 |
| 20260721 | 0.7179700460092648 | 30 | 0 | 0 |

两组 metrics 和 60 条逐实例记录均与 summary、diagnostics 完全相同；
actor/value 参数 hash 与 checkpoint metadata 相同。两个
`ppo_training_state.npz` 均可在 `allow_pickle=False` 下读取，
`completed_epoch=2`、seed 正确、`test_accessed=false`。

## 5. 物理无 test 的正式预算复跑

B 复用 P1-A02 独立复核时留下的隔离数据根。按冻结 benchmark manifest
逐条检查后，实际物理文件为：

```text
train present      = 120
validation present = 30
test present       = 0
test missing       = 30
total JSON         = 150
```

将来源 3-seed 包按原字节复制到 detached 输出根，然后运行：

```powershell
python -m trisched train-ppo --config configs/stg_ppo_5seed.json
```

运行耗时约 491 秒，run manifest 记录
`commit=8692b61...`、`working_tree_dirty=false`、
`test_accessed=false`。复跑 manifest SHA-256 为：

```text
b23fdddfae05f9676d7e9c007261e21ff394eb4a43cc7a98b48c7dc194cc1ddb
```

复跑与 A 正式包的比较：

- 旧三 seed 的 24 个继承文件零差异；
- 新两 seed 的 BC、best/last actor/value、curve、diagnostics 和 failures
  零差异；
- 两个 training state 的所有数值数组零差异；
- training state 仅
  `contract_sha256/payload_sha256` 不同，这是提交和配置原始字节
  provenance 不同的预期结果；
- 将上述 run-specific state hash 归一化后，两份 5-seed summary 完全相同；
- 五 seed mean/std 仍为
  `0.7091897025417918/0.06410852829137781`，
  failure/illegal 仍为 `0/0`。

该复跑证明两个新增 seed 不依赖 public test 字节。

## 6. LF/CRLF 边界

detached Windows 检出把配置转换为 CRLF，工作树配置 SHA-256 为：

```text
93d4bc34e5470da0de8ba5e0341e9d2a05cde51bdd6e87ef4d40a872875a836d
```

A 正式 manifest 记录的配置 SHA-256 为：

```text
7efed847ec842b469122a87eb2cbe40c410918f61fdd7a2d99aacb74ef0b3f9c
```

B 直接读取提交 `a38c530:configs/stg_ppo_5seed.json` 的 Git blob，
重算结果正是 `7efed847...`，并且 CRLF 检出与正式
`resolved_config.json` 的归一化对象逐字段相同。因此 A 的正式 provenance
正确，不构成本任务退回项。

该差异仍是 release 可迁移性风险：发布前应冻结 line-ending 策略或同时记录
Git blob hash/规范化配置 hash，不能假设不同检出的原始文本 hash 相同。

## 7. 结论与下一步

B 同意关闭 P1-A04：

- 旧三 seed 未重训、未改写；
- 只新增冻结的 `20260720/20260721`；
- 五 seed validation gate 通过，150 个调度零失败、零非法；
- 正式包与物理无 test 独立复跑一致；
- 代码、配置、checkpoint、状态和 artifact provenance 可审计。

允许下一步运行 P1-B02 的正式 development ID/OOD evidence。公开 test 仍受
一次性 gate 禁止；本复核不构成 OOD、public test 或竞赛隐藏测试领先结论。
