# P0-09/P0-11/P0-12/P0-13 收口独立复核记录

- 日期：2026-07-18
- 复核提交：`origin/main=06d3b0cd59e7b7b65fb4b38a7882c24b2dd53bb3`
- 复核分工：B 复核 P0-09/P0-11 并完成 B 演示；A 复核 P0-12；A/B 互审 P0-13
- 结论：四项通过；P0 工程交付收口完成，最终模型包仍受 P1-A05/G3/P1-06 阻塞

## P0-09：legacy MVP 复现

B 在干净 detached worktree 重新执行 synthetic pipeline、checkpoint-only validation 和非法输入路径。冻结的非时延结果与 A 记录一致：BC epoch 2/3 accuracy 都为 1；REINFORCE 最差 epoch ratio `1.052097`、末轮 `0.999228`；确定性 validation/synthetic-test masked MLP ratio 均为 `1.0`；checkpoint-only validation 仍为 `1.0`；失败数均为 0。

checkpoint SHA-256 为 `7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`，training history SHA-256 为 `f832cdc64f8bd3e343660b758ffb7afb54507534526043e400813ff2f08194b5`。`summary.json` 含绝对 `output_dir` 和 wall-clock，跨 worktree raw hash 预期不同；本轮为 `a5942511...9450a3`，不能拿 raw summary hash 冒充路径无关身份。场景 ID/hash、checkpoint、训练轨迹和全部非时延统计一致。

非法 fixture 返回 exit 2、`missing_field@$.bandwidth`。脚本中的 test 是 20 个固定 seed synthetic smoke，不是公开 STG test。P0-09 通过，结论仍是“legacy MLP 复制 HEFT且训练中间不稳”，不能宣称性能提升。

## P0-11：算法与限制一致性

B 逐项对照 `learning.py`、`ppo.py`、`gnn.py`、`exact.py`、统一 evaluator、冻结配置和 P1 正式报告：

- legacy 为 16 维 HEFT imitation + episodic REINFORCE；正式 MLP/GNN 均强制删除 2 个 teacher 位，使用 14 维输入；
- PPO 的 clipped surrogate、GAE、value、target-KL、合法 `ready task × resource` mask 和 validation 选模与文档一致；
- exact solver 明确指数级且限制小图，HEFT/CPOP 的用途和复杂度表述未越界；
- ID/CCR/system/size 数字、配对 CI 解释、失败惩罚和 public-test 停止规则与冻结报告一致；
- P1-A05 只改变 rollout source，未改变模型、奖励、优化器或选模。

未发现把 development、synthetic smoke 或点估计冒充最终 test 的表述。P0-11 通过。

## P0-12：源码发布

A 从远端提交构建两次 129-file source bundle，SHA-256 均为 `00d887c4545687113fdb4998d50f5471d2b05e3f404604b62e748c5921bccce7`。内部 manifest 验证、secret/raw-data/symlink/submodule fail-closed、严格 JSON、Markdown links/fences 和源码行尾合同均通过。历史 development evidence 未改写。

P0-12 的源码/许可证/复现交付通过。模型/checkpoint/最终结果包不属于当前可关闭部分，继续受 P1-A05、G3 和 P1-06 门禁约束。

## P0-13：A/B 五分钟演示

| 项目 | A | B |
| --- | --- | --- |
| source commit | `9831b805...597213` | `06d3b0cd...3bb3` |
| 总用时 | 25.8 秒 | 24.7 秒 |
| full regression | 260 passed | 260 passed |
| invalid exit/code | 2 / `missing_field` | 2 / `missing_field` |
| pipeline/checkpoint ratio | 1.0 / 1.0，失败 0/0 | 1.0 / 1.0，失败 0/0 |
| public STG test accessed | false | false |
| demo result SHA-256 | `46575b13...2250f` | `00ddbd6d...86c9e` |

两个提交之间 `scripts/run_demo.ps1`、`trisched/`、`configs/smoke.json` 和 `tests/` 零差异；差异只在收口文档。因此两人使用相同受控演示实现，各自生成独立 evidence。演示无需网络、Docker或现场正式训练，预期错误后可继续 checkpoint-only 恢复。P0-13 通过。
