# P1-B02 评测契约与自动报告第二轮独立复核记录

- 任务：P1-B02-REVIEW2
- 原主责：成员 B
- 独立复核：成员 A
- 日期：2026-07-17
- 被复核提交：`071b861760acc86752964126ca1074732567338e`
- 结论：R1–R3 通过，关闭契约/聚合器子阶段；P1-B02 总任务保持部分完成

## 1. 复核结论与边界

A 在 `HEAD` 与 `origin/main` 同为 `071b861...`、受跟踪工作树干净的状态下开始二次复核。A 新建独立脚本和合成输入，没有导入 B 的测试文件或 fixture helper；脚本只调用公开的 contract load、report build 和 gate claim 接口，并独立手算统计、解析 CSV、逐字节重算 hash 和注入故障。

首轮退回的三项缺陷均已关闭：

1. R1：JSON 正确公开不同的逐 seed mean，新增逐 seed CSV，四份声明 artifact 均进入 manifest；
2. R2：claim/final 均拒绝非冻结 basename，receipt payload 内的 `receipt_name` 也被 final 校验；
3. R3：契约冻结 `utc_seconds_z`，claim/final 均严格解析 canonical 秒级 UTC。

因此 P1-B02 的“契约/聚合器子阶段”通过双人复核。P1-B02 总任务仍保持“部分完成”：真实 OOD materializer、只读 evidence producer、5-seed masked-MLP release artifact 和 loader/release 一次性门禁尚未完成。本轮没有读取公开 test 原始文件，没有生成真实 OOD 场景，没有加载 checkpoint，也没有启动训练。

## 2. 独立输入与 R1 手算

A 独立构造 2 个 development 场景，并让 masked MLP 的两个 seed 故意具有不同均值：

| seed | 两个 score ratio | 手算 mean | JSON | 逐 seed CSV |
| ---: | --- | ---: | ---: | ---: |
| 101 | `0.4 / 0.8` | `0.6` | `0.6000000000000001` | `0.6000000000000001` |
| 202 | `1.2 / 1.6` | `1.4` | `1.4` | `1.4` |

浮点表示差异在 `1e-15` 内。JSON 的 seed 顺序稳定为 `101, 202`；逐 seed CSV 共 6 行，其中主策略 2 行。主策略逐 seed 行还独立核对：

| seed | P50/P95 ratio | runtime mean/P50/P95 | success/failure | valid rate | illegal/timeout |
| ---: | --- | --- | --- | ---: | --- |
| 101 | `0.6 / 0.78` | `5.0 / 5.0 / 5.9` | `2 / 0` | `1.0` | `0 / 0` |
| 202 | `1.4 / 1.58` | `9.0 / 9.0 / 9.9` | `2 / 0` | `1.0` | `0 / 0` |

全部字段与 A 的独立计算一致。

## 3. 正常统计与首轮回归

合并四条 masked MLP 记录后，A 手算并核对：

| 指标 | A 独立值 | 报告值 | 结论 |
| --- | ---: | ---: | --- |
| mean ratio | `1.0` | `1.0` | 一致 |
| seed-mean population std | `0.4` | `0.4` | 一致 |
| P50/P95 | `1.0 / 1.54` | `1.0 / 1.54` | 一致 |
| runtime mean/P50/P95 | `7.0 / 7.0 / 9.7` | `7.0 / 7.0 / 9.7` | 一致 |
| win/tie/loss vs HEFT | `2 / 0 / 2` | `2 / 0 / 2` | 一致 |

A 独立循环执行 4,096 次、seed `1707` 的 seed→scenario 重采样：

- masked MLP mean ratio 95% CI：`[0.4, 1.6]`；
- masked MLP − Random 配对 95% CI：`[-0.6, 0.6000000000000001]`。

两项均与报告逐字段一致。含一个 timeout/illegal failure 的 BC 仍得到 `mean_ratio=5.6`、`failure_rate=0.5`、`valid_schedule_rate=0.5`、`illegal_action_count=2`、`timeout_rate=0.5`，证明失败分母没有回归。

## 4. 四份声明 artifact 独立审计

A 不调用生产 `file_sha256`，按原始字节独立重算：

| artifact | bytes | SHA-256 |
| --- | ---: | --- |
| `evaluation_report.json` | 9,674 | `70d847b46028009d96bcd44289b02b1d565cdd7ef33b86c7eb148769b262746a` |
| `evaluation_per_slice.csv` | 780 | `26249eed20808042e5660119b88e660a5a28e4df5e2718a2abeb970378a20954` |
| `evaluation_per_seed.csv` | 837 | `2bd7cb48bffe29dff6d828160e80e5cab09a9b52022bc55356cb6549ca4ce61b` |
| `evaluation_primary_comparisons.csv` | 544 | `deed5971204234d6429e81cd336013472df47247f0776ef02280bbbeecfbe375` |

manifest 的 artifact 集合恰好等于上述四份文件，bytes 与 SHA-256 全部一致。

## 5. R2：receipt basename 复核

复核结果：

- 契约中的 `receipt_name=../receipt.json` 在加载时以 `report_contract / $.test_gate.receipt_name` 拒绝；
- 同一授权尝试 `receipt-one.json`、`receipt-two.json`，均以 `report_test_gate / $receipt` 拒绝且未创建文件；
- 正确 claim 写入 `receipt_name=public_test_gate_receipt.json`；
- 同一路径第二次 claim 以 `report_test_gate_consumed` 拒绝；
- final 使用错误路径 basename 时以 `report_test_gate / $receipt` 拒绝；
- 使用正确 basename、但把 receipt 内字段篡改为 `receipt-one.json` 时，以 `report_test_gate / $receipt.receipt_name` 拒绝；
- 正确 receipt 的合成 final report 可生成。

R2 的重新验收标准全部满足。

## 6. R3：严格 UTC 复核

契约将 `authorization_time_format` 改为 `free_text` 时，以 `report_contract / $.test_gate.authorization_time_format` 拒绝。claim 还逐一拒绝：

```text
not-a-timestamp
2026-07-17T00:00:00
2026-07-17T00:00:00+00:00
2026-02-30T00:00:00Z
2026-07-17T00:00:00.000Z
2026-7-17T00:00:00Z
```

合法闰日 `2028-02-29T23:59:59Z` 被接受并原样写入 receipt。final receipt 的时间被篡改为任意文本后，以 `report_test_gate / $receipt.authorized_at_utc` 拒绝。R3 的重新验收标准全部满足。

## 7. 故障注入与 CLI

除 R1–R3 专项外，A 重新执行首轮的缺失/额外 seed、重复行、scenario hash、records hash、HEFT failure/ratio、错误 penalty、development test、final 无 receipt/dirty commit 等路径。二次脚本共记录 26 个结构化拒绝结果，code/path 均符合契约。

CLI 对 records hash 漂移继续返回 exit `4`、stdout 为空，stderr 为 `report_hash / $.records_sha256`。正常统计、bootstrap、失败分母、final 合成 gate 和 CLI 均未发生回归。

## 8. 完整回归与证据

执行：

```powershell
python -m black --check trisched/reporting.py trisched/cli.py tests/test_p1_b02_reporting.py outputs/p1-b02-second-review.py
python -m compileall -q trisched
python -m pytest -o addopts= -q tests/test_p1_b02_reporting.py
python -m pytest -o addopts= -q
git diff --check
```

结果：专项 `28 passed in 0.87s`，完整回归 `216 passed in 10.87s`；Black、compile 和 Git whitespace check 均通过。

A 的独立脚本和机器报告保存在 Git 忽略的 `outputs/`，不提交仓库：

```text
outputs/p1-b02-second-review.py
bytes=30071
sha256=263ac6fa1094a7567db0dc098d6ba3b2cfe476e8296666caec67b6ee84640ab0

outputs/p1-b02-second-review/review_report.json
bytes=10573
sha256=fd6fda9edc14a101c1bc7cdd1fbeb4b40c78df6873f27b925c379b1e955909c4
```

## 9. 已知边界与下一门禁

本轮有意验证：同一个冻结 basename 放在另一个目录时仍能再次 claim。这不属于 R2 的“执行冻结文件名”缺陷，但证明 gate 仍只是本地路径工作流保护，不是全局 append-only ledger。以下边界继续有效：

- OOD 场景 manifest 尚未由 materializer 从真实 Scenario 字节生成；
- 同 basename 的不同目录、删除 receipt 后重领仍需固定 loader/release root 和不可覆盖 artifact 约束；
- public-test loader 尚未在读取前直接调用 claim；
- 5-seed masked-MLP release artifact 尚未补齐；
- 没有真实 OOD/public-test 性能结果。

下一主任务交回成员 B：实现确定性的 OOD materializer 与只读 evidence producer，先冻结 `scenario_id + scenario_hash` 和 source manifest，仍不得访问 public test。完成并由 A 复核后，才进入 5-seed development/OOD 结果阶段。
