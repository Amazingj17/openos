# P0-03 Scenario 输入契约独立复核记录

- 日期：2026-07-16
- 原主责：成员 B
- 复核人：成员 A
- 远端基线：`207b8eb08b5db64d954f77c215c04999776f825a`
- 结论：通过

## 1. 复核边界与独立性

本次复核只判断 Scenario 输入契约是否能稳定拒绝非法输入并向 Python/CLI 调用方提供可机器处理的诊断，不重新证明调度算法、训练方法或模型性能。

成员 A 新建 `tests/test_scenario_review.py`，采用以下独立方法：

- 不读取或参数化 `tests/fixtures/invalid/scenario_cases.json`；
- 不调用 `generate_scenario`，手写一个三任务、双资源的合法 payload；
- 每类异常从合法 payload 单独变异，不复制 B 的固定非法 fixture；
- Python API 直接检查 `Scenario.from_dict/load` 抛出的 `code/path/to_dict()`；
- CLI 使用 `sys.executable -m trisched validate-scenario` 真实子进程，检查进程退出码、标准错误和 JSON 标准输出。

## 2. 独立异常结果

| 独立构造 | Python API 结果 | CLI 结果 | 结论 |
| --- | --- | --- | --- |
| 删除顶层 `latency` | `missing_field` / `$.latency` | 退出码 2，同 code/path | 通过 |
| 在首个 task 增加 `review_note` | `unknown_field` / `$.tasks[0].review_note` | 退出码 2，同 code/path | 通过 |
| task ID 使用 JSON `true` | `type_error` / `$.tasks[0].id` | 退出码 2，同 code/path | 通过 |
| workload 使用原始 JSON 数字 `1e309` | `non_finite` / `$.tasks[0].workload` | 退出码 2，同 code/path | 通过 |
| bandwidth 第二行缺一列 | `matrix_shape` / `$.bandwidth` | 退出码 2，同 code/path | 通过 |
| 重复边 `0 -> 1` | `duplicate_edge` / `$.edges[2]` | 退出码 2，同 code/path | 通过 |
| 增加 `2 -> 0` 形成环 | `cycle` / `$.edges` | 退出码 2，同 code/path | 通过 |
| 使用 GBK 字节保存含中文的 JSON | `encoding_error` / `$`（`Scenario.load`） | 退出码 2，同 code/path | 通过 |

`1e309` 的 API 路径用其解析结果正 Infinity 构造，CLI 路径则把字面量 `1e309` 直接写入 JSON，确认溢出诊断落在具体 workload 字段而不是根路径。所有非法 CLI 调用均无标准错误输出，错误对象的 `message` 为非空字符串。

## 3. 合法输入与 CLI 成功契约

同一个手写 payload 可由 `Scenario.from_dict` 正常构造。CLI 返回退出码 0、空标准错误，并输出：

- `valid = true`；
- `scenario_id = "a-review"`；
- `task_count = 3`、`resource_count = 2`；
- `content_hash` 与 Python API 计算结果一致。

## 4. 自动化与回归证据

复现命令：

```powershell
python -m pytest -o addopts="" -q tests/test_scenario_review.py
python -m pytest -o addopts="" -q
python -m trisched pipeline --config configs/smoke.json --output outputs/p0-03-review-smoke
python -m trisched evaluate --config configs/smoke.json --checkpoint outputs/p0-03-review-smoke/masked_mlp.npz --split test --output outputs/p0-03-review-eval
```

实际结果：

- 独立复核文件：`16 passed in 2.14s`；
- 完整测试：`139 passed in 2.67s`；
- pipeline：20 个 test 实例，masked MLP `mean_ratio = 1.0000`、合法率 `1.0`；
- checkpoint 复评：20 个 test 实例，`mean_ratio = 1.0000`、合法率 `1.0`，未重新训练；
- checkpoint SHA-256：`7fb644af6ae06530fdac556fd07fc4281a560646d16c2ab5f00d2641fbd8f2b7`；
- `tests/test_scenario_review.py` SHA-256：`01c650c4ab1b1dc0f643946e0fd20fe13d5f58da87e5c63bb25a18d111177c5c`。

远端实现基线的 GitHub Actions run <https://github.com/Amazingj17/openos/actions/runs/29503822505> 已完成；Ubuntu/Windows × Python 3.10/3.11/3.12 六个 job 全部 `success`。

## 5. 结论与边界

成员 A 同意 P0-03 改为“已完成”：B 冻结的稳定错误类别、字段路径、CLI JSON 和退出码契约均通过独立异常复核，且未造成既有测试、pipeline 或 checkpoint 复评回归。

该结论不表示 JSON Schema 能单独验证跨字段不变量，也不改变现有静态 DAG 模型边界。调用方仍必须经过 `Scenario.from_dict/load`；未来增加不兼容字段前仍需先定义格式版本和迁移规则。

P0 正确性任务至此具备召开 G1 的条件。下一项是 B 汇总 G1 门禁证据、A 独立签收；G1 完成后执行 P0-10 openEuler CPU smoke，之后才进入公开 benchmark、PPO 和 task-GNN。
