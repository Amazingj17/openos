# P1-A01 HEFT teacher 与公开 STG BC 基线独立复核记录

- 任务：P1-A01 / P1-02
- 原主责：成员 A
- 独立复核：成员 B
- 日期：2026-07-17
- 被复核远端提交：`180a7130517df425bf53dab945f3760e5bcdba72`
- 实现提交：`2eda5ae0eeca87affd54f4067ba17d4cdff1f766`
- 远端运行：<https://github.com/Amazingj17/openos/actions/runs/29565244968>
- 结论：通过；P1-A01 / P1-02 改为已完成（B 独立复核通过）

## 1. 复核边界与方法

B 在 detached worktree 中复核远端不可变提交，没有复用 A 的 `outputs/p1-a01-*`。复核重新下载官方固定 archive，建立只含 120 个 train 和 30 个 validation JSON 的 raw root，并删除全部 30 个 test JSON。随后在完全缺少 test 原始字节的前提下连续训练两次，独立重算 teacher、checkpoint、validation 与 run manifest 证据。

临时原始数据、checkpoint、注入文件和运行结果均位于 Git 忽略的 `outputs/`，不作为仓库交付物提交。

## 2. 远端提交与 CI

复核时远端 `main` 指向 `180a7130517df425bf53dab945f3760e5bcdba72`。对应 Actions run 的以下 7 个 job 全部成功：

- Windows × Python 3.10 / 3.11 / 3.12；
- Ubuntu × Python 3.10 / 3.11 / 3.12；
- openEuler 24.03 LTS SP4 × Python 3.11。

该 run 的 7 个 artifact 均存在且未过期，记录的到期日为 2026-07-31。artifact 只有短期留存作用，长期结论以仓库内复核记录、固定输入 hash 和可重生成命令为准。

## 3. 完全无 test 字节的双次重跑

重新下载的官方 archive SHA-256 为：

```text
03bc163c13ae8601f8cb20ac1573746a5262c50edbf0c4e9e748968675ea5f7d
```

删除 test 后的独立 raw root 计数为：

```text
train_present=120/120
validation_present=30/30
test_present=0/30
remaining_files=150
```

在该 raw root 上连续两次执行 `train-bc`，耗时约 185.6 秒和 184.6 秒。下列 10 个核心构件两次逐字节相同，并与 A 记录的正式值一致：

```text
resolved_config.json
train_teacher_manifest.json
validation_reference_manifest.json
teacher_failures.jsonl
bc_training_curve.json
bc_best.npz
bc_last.npz
validation_failures.jsonl
bc_validation_diagnostics.json
bc_summary.json
```

两个 `bc_run_manifest.json` 仅因 `created_at_utc` 不同而具有不同文件 hash：

```text
run1: ec46c9aeeee3f3c51f0fbcb65471c97254856ff55933bbb6376f53f653057dd0
run2: 93734baba61fb2e4e4c105efd775a745fd1ded9235e171dbd2902dbee7a1de71
```

两份 manifest 都记录 `commit=180a713...`、`working_tree_dirty=false` 和 `test_accessed=false`。B 对每份 manifest 声明的 10 个 artifact 重新计算文件大小和 SHA-256，全部一致。

## 4. teacher 与 validation reference

独立重跑得到：

| 对象 | Scenario | 动作 | 失败 | trace hash 聚合 | schedule hash 聚合 |
| --- | ---: | ---: | ---: | --- | --- |
| train teacher | 120 | 6,000 | 0 | `51acb3c9efb00a18571ebe8c7b9723f36035044571f78939ddb40b7f799e4d69` | `96859e209bfe9091aac81f6fc705c686ccf29dd4611d775ccf4e22560c23e29c` |
| validation reference | 30 | 1,500 | 0 | `29466ecdc92c0591f1d3efd3b4171ffc67684caf150f702a39dd2bf5d3eea75f` | `b4e5f5a0a588aafa2dbaa1e60c2df3ff9ff2fb8e92a9742b38d739dd5b6337cf` |

B 按固定索引 `0, 13, 26, 39, 52, 65, 78, 91, 104, 119` 抽查 10 个 train 场景。每例均重新比较 teacher manifest 动作序列、生产 HEFT、独立 HEFT、makespan、trace hash、schedule hash 及两套 validator，10/10 全部一致。

## 5. checkpoint 与 validation 复评

双次重跑产生的 NPZ 与参数 hash 均与 A 的正式值一致：

| checkpoint | NPZ SHA-256 | parameter SHA-256 |
| --- | --- | --- |
| best | `fa5fa524242d58c46173211bb72ee168e8d7a7302274ff82bbcf67cd174e7394` | `fb3bba126058c3e4053d290e753a065bfa58f7ffe1c64c8ed83b6310b5d15388` |
| last | `1239ac1fd4edfdf8dfe14c0b938c9e90d6bdeff26629b39f1f8c0d7e6bac6dbf` | `343fde145197a0711dd61b9f00d73b975b8a5454da35c403132991f49b9e3f6b` |

B 分别加载 best 和 last，对 30 个 validation 场景重新 rollout 并执行双 validator。两者均得到：

```text
success=30/30
mean_ratio=1.0
failure_count=0
valid_schedule_rate=1.0
illegal_action_count=0
illegal_action_rate=0.0
teacher_action_accuracy=1.0
```

best 与 last 参数 hash 不同，但二者在当前 validation 上都复制 HEFT；这验证了选模和加载链路，不构成优于 HEFT 的证据。

## 6. 防泄漏与故障注入

B 没有仅依赖正常路径，而是执行以下负向检查：

| 注入 | 预期且实际错误码 |
| --- | --- |
| test 用作 teacher | `split_usage` |
| test 用作 training | `split_usage` |
| test 用作 model selection | `split_usage` |
| 篡改 teacher trace hash | `teacher_trace_hash` |
| 将动作改为非法 `[999, 0]` 并同步重算 trace hash | `teacher_illegal_action` |

三项 test 用途均在读取 test JSON 前被拒绝。注入前后独立 raw root 始终只有 150 个 JSON，`test_json_present=0`，证明成功训练和错误检查都不依赖 test 字节。

## 7. 完整回归与工作树

在 detached worktree 执行：

```powershell
python -m pytest -o addopts= -q
git status --porcelain --untracked-files=all
```

结果为 `165 passed in 5.57s`；随后工作树状态为空，没有因复核产生待提交文件。

## 8. 结论与剩余限制

B 同意 P1-A01 / P1-02 通过独立复核。通过含义仅包括：公开 STG train/validation 的 teacher、纯 BC、best/last、用途门禁、双 validator 和可重生成证据链已经闭合，可以开始下一主任务 P1-A02 masked PPO。

以下限制继续生效：

- 公开 test 完全未访问，本记录没有 test 结果；
- `is_heft_task` 和 `is_heft_pair` 直接暴露教师决策，validation ratio 1.0 只能表述为复制 HEFT；
- 当前只有单训练 seed，没有多 seed、置信区间、ID/OOD 或性能领先证据；
- 输入仍是 capability-relaxed STG topology projection，不代表完整 GrapheonRL 异构约束复现；
- P1-A02 必须先冻结移除/保留 teacher 二值特征的消融，再在相同数据和评测契约上实现 masked PPO，test 继续禁止参与选模。
